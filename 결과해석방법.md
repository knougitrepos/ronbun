# 실험 결과 해석 방법

이 문서는 다음 두 노트북이 생성하는 산출물의 위치, 역할, 검증 방법과 해석 순서를 정리한다.

- `notebooks/common/orchestration/00_batch_experiment_runner.ipynb`
- `notebooks/common/reports/00_cross_dataset_results.ipynb`

## 1. 먼저 알아둘 구조

두 노트북은 같은 결과를 중복 생성하지 않는다.

| 구분 | Batch experiment runner | Cross-dataset results |
|---|---|---|
| 역할 | 모델별·데이터셋별 Step 4 실험과 후처리 실행 | 완료된 run을 선택하여 통합 표·그림 생성 |
| 원본 결과 위치 | `runs/<dataset>_<date>/<run_id>_step4_<dataset>_<model_uid>/` | 원본 run을 직접 변경하지 않음 |
| 요약 결과 위치 | `results/paper/<dataset>/<run_id>/search_space_v4_multi_fpir/` | `results/paper/common/<model>/<dataset-run 조합>/` |
| 검증 기준 | `COMPLETED`, `run_manifest.json`, phase manifest | 선택 run의 완료 상태, 모델 UID, run ID, summary manifest와 SHA-256 |
| 해석 단위 | 한 모델 × 한 데이터셋 | 한 모델 × 선택한 한 개 이상 데이터셋 |

완료된 run은 원자료이므로 수정하거나 삭제하지 않는다. 후처리 결과와 통합 보고서는 원 run을 참조하는 파생 artifact이며, 매니페스트에 기록된 source run 및 SHA-256이 일치할 때만 사용한다.

## 2. Batch experiment runner 결과

### 2.1 위치와 완료 확인

실험의 기본 결과는 아래와 같은 경로에 있다.

```text
runs/<dataset>_<YYYYMMDD>/<run_id>_step4_<dataset>_<model_uid>/
```

예시:

```text
runs/lfw_20260811/
  20260811-R001-c2e11ab2_step4_lfw_adaface-4df25b75e065b0b9ed43/
```

이 폴더를 분석에 사용하기 전에는 반드시 다음을 확인한다.

1. `COMPLETED` 파일이 존재한다.
2. `run_manifest.json`의 `status`가 `completed`이다.
3. `phases/` 안의 phase manifest에 중단된 마지막 단계가 없는지 확인한다.
4. `run_manifest.json`의 모델 UID, config hash, Git/source snapshot이 비교 대상과 호환되는지 확인한다.

`running` 상태이거나 `COMPLETED`가 없는 run은 완료 결과로 섞지 않는다. 중단 원인이나 재개 가능성을 판단할 때에는 notebook 출력보다 `phases/`와 manifest를 우선한다.

### 2.2 원 실험 artifact

주요 파일은 다음 위치에 있다.

```text
runs/.../artifacts/step2_workflow/
```

| 파일 또는 폴더 | 의미 | 해석 방법 |
|---|---|---|
| `step4_summary.json` | 표본 수, Grad-CAM 대상 수, retrieval row 수, codec 수, calibration 상태를 담은 간결한 요약 | 가장 먼저 확인한다. 표본 수가 다른 결과를 같은 수준의 실험처럼 비교하지 않는다. |
| `paired_embedding_metrics.csv` | 원본 embedding과 PCA/PQ 압축·복원 embedding의 쌍별 기하 오차 | 평균 각도 오차가 작고 원본 cosine이 클수록 표현 보존이 좋다. 그러나 기하 오차가 작다는 사실만으로 검색 성능 유지가 증명되지는 않는다. |
| `retrieval_metrics.csv` | 1:N open-set 검색의 원본/압축 결과 | `compressed_dir_rank1`, `compressed_fpir`, 원본 대비 차이, false-accept count/denominator, 신뢰구간을 중심으로 본다. |
| `origin_open_set_score_audit.csv` | 원본 점수의 calibration/test FPIR 점검 | calibration에서 정한 threshold가 test에서도 목표 FPIR를 만족하는지 확인한다. |
| `saliency_population/` | Grad-CAM heatmap과 선택 표본 메타데이터 | 설명 가능한 시각화의 원자료다. heatmap 자체를 성능 개선 또는 인과관계의 증거로 해석하지 않는다. |
| `saliency_validation.json` | landmark/mask 기반 saliency 품질 검사 | coverage 부족, semantic region 결측, 비정상 mask가 있으면 해당 영역 기반 해석은 보류한다. |
| `saliency_geometry_associations.csv` | saliency 특성과 압축 기하 왜곡의 연관 | 어떤 주의 영역을 가진 표본에서 왜곡이 관찰되는지 기술한다. 상관관계이지 인과관계는 아니다. |
| `saliency_retrieval_associations.csv` | saliency 특성과 검색 결과 변화의 연관 | rank 변화나 accept/reject 변화와의 관계를 확인한다. dataset, threshold policy, search mode를 함께 기록한다. |
| `representative_cases.csv` | 유지·악화·판정 변경의 대표 사례 | 수치 표에서 발견한 현상을 시각적으로 보조 설명할 때 사용한다. 전체 결론은 요약 retrieval 지표에 둔다. |
| `frozen_codec_manifest.json` | PCA/PQ codec의 출처, fit 조건, SHA-256 | 같은 codec 계보인지 확인한다. 서로 다른 source run에서 fit한 codec 결과를 무조건 동일 조건으로 비교하지 않는다. |
| `freeze_manifest.json` | 입력과 추출 산출물의 고정 기록 | 재실행과 파생 평가가 어느 원 run을 근거로 하는지 확인한다. |

### 2.3 dataset별 compact search-space summary

Batch runner의 후처리가 성공하면 다음 경로가 생긴다.

```text
results/paper/<dataset>/<run_id>/search_space_v4_multi_fpir/
```

핵심 파일은 다음과 같다.

| 파일 | 의미와 사용법 |
|---|---|
| `summary_manifest.json` | 가장 중요한 검증 파일이다. `artifact_type=step4_search_space_multi_fpir_v4`, source run ID, model UID, 목표 FPIR, 원 run 보존 여부, 출력 SHA-256을 확인한다. `claim_status=validation_required`는 계산 실패가 아니라 최종 논문 주장 전의 검증 조건이 남았음을 뜻한다. |
| `compression_summary.csv` | PCA/PQ 프로파일별 저장량, codebook 비용, 각도 오차, 원본과의 유사도를 요약한다. 저장 효율과 표현 왜곡의 trade-off를 본다. |
| `retrieval_summary.csv` | PCA/PQ 프로파일별 DIR@Rank-1, FPIR, 원본 대비 변화량, false-accept count/denominator, Wilson 신뢰구간, paired bootstrap 차이를 요약한다. open-set 성능 판단의 중심 표다. |
| `pca/`, `pq/` | 압축 계열별 summary, family manifest, origin calibration diagnostics가 있다. 계열별 score space와 threshold policy를 확인한다. |

## 3. Cross-dataset results 결과

### 3.1 위치와 선택 규칙

`00_cross_dataset_results.ipynb`는 `MODEL_NAME`, `DATASETS`, `MODEL_UIDS`, `RUN_IDS`로 이미 완료된 run을 명시 선택한다. 최신 결과를 자동 선택하지 않는다.

출력 경로는 다음 규칙을 따른다.

```text
results/paper/common/<model_name>/<dataset>-<run_id>__<dataset>-<run_id>.../
```

예시:

```text
# LFW AdaFace 한 개 run
results/paper/common/adaface/lfw-20260811-R001-c2e11ab2/

# LFW + SurvFace + RFW-Custom AdaFace 통합 선택
results/paper/common/adaface/
  lfw-20260811-R001-c2e11ab2__survface-20260811-R001-cb329ad5__rfw_custom-20260811-R001-84619ab9/
```

동일한 선택 조합을 `OVERWRITE_COMMON_OUTPUTS=True`로 다시 실행하면 같은 경로의 결과를 덮어쓴다. 반대로 dataset/run 조합이 달라지면 별도의 content-addressed 폴더가 생성된다.

### 3.2 통합 보고서 파일

| 파일 | 의미와 사용법 |
|---|---|
| `cross_dataset_summary_manifest.json` | 통합 보고서의 기준 파일이다. 선택한 run, model UID, dataset별 summary manifest SHA-256, 출력 파일 SHA-256, protocol boundary와 보류된 주장 정보를 확인한다. 먼저 읽는다. |
| `compression_summary_all.csv` | 선택 dataset 전체의 압축 왜곡·저장량 요약이다. 동일 모델·동일 압축 프로파일의 dataset별 경향을 비교할 수 있다. |
| `retrieval_summary_all.csv` | 선택 dataset 전체의 1:N open-set 핵심 결과다. DIR, FPIR, 원본 대비 차이, CI, threshold policy, search mode를 함께 확인한다. |
| `dir_fpir_storage_latency.csv` | 저장량, 지연, DIR, FPIR를 한 표에서 비교한다. 배포 후보 압축 프로파일의 실용적 trade-off를 판단할 때 사용한다. |
| `lfw_survface_fpir_appendix.csv` | LFW/SurvFace의 10%와 1% FPIR operating point를 부록 형태로 정리한다. |
| `saliency_geometry_associations_all.csv` | 선택 dataset의 saliency–기하 왜곡 연관을 합친 표다. |
| `saliency_retrieval_associations_all.csv` | 선택 dataset의 saliency–검색 변화 연관을 합친 표다. |
| `representative_cases_all.csv` | 논문 그림 또는 사례 분석 후보를 모은 표다. |
| `storage_distortion.png` | x축은 embedding당 저장 payload, y축은 평균 각도 오차다. 왼쪽·아래 방향일수록 저장량·왜곡 기준에서는 좋다. 검색 성능은 이 그림만으로 판단하지 않는다. |
| `storage_open_set.png` | 저장량 대비 압축 DIR@Rank-1과 FPIR를 보여 준다. 목표 FPIR 조건에서 DIR 저하가 작은 프로파일을 찾는 그림이다. |
| `survface_faithfulness_cross_model.csv` | 존재하는 경우에만 사용한다. Grad-CAM occlusion faithfulness의 보조 분석이며, threshold-independent 결과인지와 manifest의 source run 일치를 확인한다. |

## 4. 권장 해석 순서

1. **완료와 계보 확인**
   - 원 run의 `COMPLETED`와 `run_manifest.json["status"] == "completed"`를 확인한다.
   - 모델 UID, run ID, dataset ID, config/source snapshot을 비교 대상과 맞춘다.

2. **표본 범위 확인**
   - `step4_summary.json`에서 `selected_samples`, saliency 대상 수, retrieval row 수를 확인한다.
   - full/quick, 데이터 분율, gallery 구성, calibration population이 다른 결과를 하나의 공정 비교로 섞지 않는다.

3. **요약 artifact 검증**
   - dataset summary의 `summary_manifest.json`에서 source run ID, model UID, `target_fpirs`, output SHA-256을 확인한다.
   - 통합 보고서의 `cross_dataset_summary_manifest.json`에서 선택한 모든 run과 dataset summary SHA-256이 일치하는지 확인한다.

4. **검색 성능을 우선 해석**
   - `retrieval_summary.csv` 또는 `retrieval_summary_all.csv`에서 같은 `target_fpir`, `search_mode`, `threshold_policy` 안에서 비교한다.
   - `compressed_dir_rank1`은 높을수록 좋고, `compressed_fpir`은 목표 FPIR 이하인지 본다.
   - 원본 대비 차이는 `compressed_minus_origin_dir_rank1`, `compressed_minus_origin_fpir` 및 대응 신뢰구간으로 해석한다.
   - false-accept count와 denominator를 함께 확인한다. 작은 분모에서의 FPIR 점추정치만으로 강한 결론을 내리지 않는다.

5. **압축 효율과 함께 판단**
   - `compression_summary`에서 저장량과 왜곡을 본다.
   - `dir_fpir_storage_latency.csv`와 `storage_open_set.png`로 저장량·속도·DIR·FPIR를 함께 보고 후보 프로파일을 정한다.
   - 낮은 각도 오차가 낮은 DIR 손실을 보장하지 않으므로, 기하 지표와 retrieval 지표를 분리해 제시한다.

6. **saliency와 사례는 보조 근거로 사용**
   - association CSV와 representative cases는 어떤 조건에서 변화가 관찰되었는지를 설명하는 데 사용한다.
   - 이 결과는 관찰된 연관이며, 모델 구조·loss·Grad-CAM 영역이 성능 변화를 일으켰다는 인과적 증거로 확대 해석하지 않는다.

## 5. 비교 시 반드시 지킬 경계

### PCA/PQ와 threshold policy

- PCA와 PQ는 압축 방식과 score space가 다를 수 있다.
- 특히 PQ ADC 등 압축 score space가 cosine과 다르면 원본 cosine threshold를 그대로 이식하지 않는다.
- `frozen_origin`과 `recalibrated_compressed` threshold policy를 분리하여 표와 문장에 명시한다.
- 같은 target FPIR, search mode, threshold policy가 아닌 행은 직접적인 성능 우열 비교 대상으로 쓰지 않는다.

### LFW/SurvFace와 RFW의 프로토콜 경계

- LFW와 SurvFace의 본 표는 1:N open-set 결과이며, DIR/FPIR/rank 기반으로 해석한다.
- RFW-Custom도 1:N open-set 결과일 때만 같은 종류의 표에 포함할 수 있다.
- RFW-Official은 1:1 verification 결과다. Accuracy/TAR/FAR/EER와 group gap은 LFW/SurvFace의 DIR/FPIR 표나 축에 합치지 않고 별도 frozen-codec evaluation으로 보고한다.

## 6. 결과 문장 작성 예시

좋은 기술 문장은 압축률만 말하지 않고 운영점과 불확실성을 함께 적는다.

```text
LFW의 <동일 target FPIR, search mode, threshold policy> 조건에서
<압축 프로파일>은 embedding payload를 <값>으로 줄였고,
원본 대비 DIR@Rank-1 변화는 <값>이었다.
Compressed FPIR는 <값> (<false accepts>/<denominator>)이며,
해당 차이의 신뢰구간은 <값>이다.
```

다음과 같은 과장은 피한다.

- Grad-CAM이 특정 부위를 강조했으므로 그 부위가 성능 차이를 만들었다.
- 각도 오차가 작으므로 검색 성능도 유지된다.
- 한 dataset 또는 한 calibration 조건의 결과만으로 일반화 성능이 증명된다.
- RFW-Official의 1:1 TAR/FAR/EER과 open-set DIR/FPIR을 한 숫자 체계처럼 결합한다.

## 7. 재실행 또는 중단 후 점검

중단 후 재실행하기 전에는 다음 순서를 따른다.

1. `run_manifest.json`, `COMPLETED`, `phases/`로 원 run 상태를 확인한다.
2. 완료 run이면 원 run을 다시 실행하거나 삭제하지 않고 후처리만 재실행할 수 있는지 확인한다.
3. 통합 보고서가 중단되었더라도 `cross_dataset_summary_manifest.json`과 모든 output file의 존재·SHA-256을 확인한다.
4. 같은 선택 조합에서 `OVERWRITE_COMMON_OUTPUTS=True`로 정상 종료했다면 기존 공통 보고서는 덮어써진 것이므로 별도 중복 결과가 생기지 않는다.
5. 불완전한 파생 보고서를 삭제해야 할 때에도, 원 `runs/...` 완료 artifact와 `results/paper/<dataset>/<run_id>/` dataset summary는 삭제하지 않는다.
