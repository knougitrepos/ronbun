# 2026-09-17 — 02 calibration saliency 생성·검증 연결

- 상태: **구현됨 / 회귀 테스트·ArcFace 소규모 GPU 실행 검증됨**. 전체 calibration 생성 및 정식 02 성능 비교는 미실행이다.
- 기준: `step12`, HEAD `aee46c75a727642e71389064b033ae4bf08c0aee`와 작업 중 변경.
- 변경 배경: 20260916 기록의 02는 입력 검증·조건부 비교만 구현되어 calibration용 saliency를 생성할 수 없었다. 사용자 요청에 따라 같은 노트북에 생성 경로를 연결한다.
- 이전 결정과의 관계: split-matched gallery, 원본 run 불변, High/Low/Random의 검증 전용 역할은 유지한다. producer 미구현 상태와 기본 실행 플래그 False를 갱신한다. 검증되지 않은 development 자료도 사용할 수 있다는 이전 범위는 축소하여, 현재 condition에 실제로 속하는 calibration 근거만 승인한다.

## 결정과 데이터 흐름

완료 source run + condition(v2) → frozen model/정렬 이미지/landmark/embedding/protocol 검증 → split별 원본 gallery top1 복원 → calibration Grad-CAM 특징 생성 + calibration 사전 선정 표본의 가림 실험 → 기존 test 특징 검증·재사용 → 입력 파일·manifest 저장 → coverage/faithfulness gate → 통과할 때만 Continuous FIQA 기준 및 saliency 추가 비교 → 단일 split·20개 split 저장.

1. 신규 `research/experiments/saliency_calibration_inputs.py`가 생성 책임을 갖고, 02 노트북은 첫 셀 설정과 실행·표시를 담당한다. 기존 00/01, batch runner, DB, checkpoint, 완료 run과 기존 실험 결과는 변경하지 않는다.
2. CUDA를 요구한다. 실제 device/torch/CUDA runtime을 기록하고 CPU로 조용히 전환하지 않는다. 기본 Grad-CAM batch 4, 처리 묶음 128, 가림 inference batch 32이다. CUDA/PyTorch 패키지를 업그레이드하지 않는다.
3. 기존 calibration seed와 gallery identity 수로 calibration protocol을 복원한다. Test는 official protocol을 사용한다. Query ID·identity·정렬 hash, source/freeze/selected/prepared/model/checkpoint/preprocessing/landmark lineage가 다르면 중단한다.
4. Grad-CAM target은 각 split의 **원본 cosine top1 gallery template**이다. 정답 label이나 ADC score를 gradient target으로 쓰지 않는다. 생성 forward와 frozen embedding cosine 및 target score를 대조한다. 기존 dense landmark 영역 정의로 `outside_face_attention`, `saliency_entropy`를 계산한다.
5. Test 특징은 기본적으로 기존 population artifact를 재사용한다. 파일 hash, 모델·전처리·target layer·region mask, 각 query의 공식 gallery top1 identity와 score를 대조한다. 불일치는 자동 무시하지 않는다. `REUSE_TEST_SALIENCY=False`는 test도 같은 방식으로 새로 생성한다.
6. 가림 실험은 **calibration에서 사전 고정한 최대 10,000장**만 수행한다. Test와 sample ID/identity/정렬 이미지 내용이 겹치는 행을 제외하고 `SHA256(seed:sample_id)` 순서로 선택한다. 기본 seed 8972, 가림 비율 0.1, Random 5회이다. `FAITHFULNESS_MAXIMUM_SAMPLES=None`이면 적격 calibration 전체를 사용한다. 결과를 보고 표본·seed·가림 비율을 바꿔 gate를 통과시키지 않는다.
7. High/Low/Random은 기존 가림 구현을 재사용하며 동일한 unoccluded top1 template을 고정한다. Score drop은 threshold 입력이 아니다. Calibration-fit/safety 및 test의 saliency 특징은 모든 query를 유지하며, 미생성·무효 특징을 임의 대치하거나 평가 cohort에서 삭제하지 않는다.
8. 저장된 실제 faithfulness 행으로 identity-cluster 95% CI를 계산한다(기본 2,000회). 소비할 때도 membership/identity/alignment/split, paired gain 수식, bootstrap 설정·identity 수·CI를 재검산한다. High−Low와 High−Random 두 CI 하한이 양수가 아니면 비교를 차단한다. 일부 target 값이 비어 있는 경우도 차단한다.
9. `max_queries_per_split`은 코드 검증용 제한이며 생성 manifest에 `smoke_only=True`를 기록한다. 제한된 입력은 정식 비교를 승인할 수 없다.
10. 01 test 결과를 참고한 사용자의 기준 선택을 반영해 기본값은 `continuous_fiqa`이다. 자동 test 기반 최적 선택은 하지 않지만, 현재 02의 해석은 **01-test-informed exploratory**임을 기록한다. 원본 gallery가 필요한 offline 추가정보 분석이며 압축 DB만으로 배포 가능함, 압축오차의 인과효과 또는 formal FPIR 보장을 주장하지 않는다.

## 파일과 artifact

- 노트북: `notebooks/calibration/02_saliency_incremental_threshold_calibration.ipynb`.
- 생성 모듈: `research/experiments/saliency_calibration_inputs.py`.
- 검증·비교 모듈: `research/experiments/saliency_incremental_calibration.py`.
- 입력 경로: `results/calibration/saliency_inputs/<run_id>/saliency-inputs-<UID>/`.
  - `calibration-*.parquet`, `test-*.parquet`, `progress.json`: 묶음별 재개 기록.
  - `saliency/saliency_features.csv`, `saliency/manifest.json`.
  - `faithfulness/faithfulness_rows.csv`, `faithfulness_summary.csv`, `manifest.json`.
  - 최상위 `manifest.json`: 완료 상태 및 출력 path/bytes/SHA.
- UID는 condition manifest/실제 frame hash, 생성 설정·runtime, 기존 test saliency manifest, 사전 선정 표본 hash, 관련 구현 hash를 포함한다. 중단 후에는 같은 UID의 완료 shard를 SHA와 cohort로 검증해 재사용한다. 이미 완료된 입력은 수정하지 않는다.
- 대용량 parquet와 원자료 CSV, 진행/임시 파일은 `.gitignore`에 추가한다. 작은 manifest와 faithfulness summary는 추적 가능하다. Git clone만으로 원자료까지 제공되지는 않는다.
- 보정 결과 경로는 기존 `saliency_incremental/<run_id>/<UID>` 및 `saliency_incremental_split_stability/<run_id>/<UID>`를 유지한다.
- 02의 과거 출력은 새 실행 경로와 혼동되지 않도록 비웠다. 01의 사용자 설정 변경과 `scratch_calibration_summary.txt`는 이번 작업에서 수정하지 않았다.

## 사용자 실행과 재실행 범위

02 첫 셀에서 `SOURCE_MODEL`을 확인하고 **Kernel Restart → Run All**한다. 기본 `BUILD_SALIENCY_INPUTS`, 단일 split RUN/WRITE, 분할 안정성 RUN/WRITE는 모두 True이다. 자료 생성 → 검증 → 통과 시 비교·저장까지 이어진다. 최초 생성은 장시간 걸릴 수 있으며 정식 소요 시간은 아직 측정하지 않았다.

재실행 시 생성 플래그를 그대로 두면 같은 완료 묶음을 검증해 재사용한다. 설정·코드가 달라 UID가 달라지면 별도 입력을 만든다. 명시적 기존 입력 경로를 쓸 때는 BUILD=False와 두 디렉터리를 함께 지정한다. BUILD=False 및 두 경로 None은 기존 자료 진단 경로다.

Gate 실패도 가림 원자료·요약·manifest는 남는다. 이때 단일/분할 안정성의 성능 결과는 생성하지 않는다. 00/01 또는 기본 임베딩 압축 실험을 다시 실행할 필요는 없다.

## 검증 결과와 미해결 위험

- 관련 회귀 **71개 통과**. 별도 전체 호출에서 01 설정을 반드시 False로 요구하는 기존 테스트 1개가 현재 사용자 설정 True 때문에 실패했다. 01 설정을 변경하지 않고 그 테스트만 제외해 71개를 재실행했다.
- 신규 producer 테스트: test ID/identity/content 제외와 순서/score 독립 표본 선정, 중단 후 완료 shard 재사용, 완료 입력 재사용, 파일 손상 차단, 제한 smoke 차단, CUDA 미가용 차단.
- Consumer 회귀: CI·bootstrap metadata·paired gain 변조, 실제 calibration에 없는 ID, 누락/null target 차단. 합성 ready 입력의 기준/추가 특징 적합, 동일 test 변경 시 학습 계수 불변과 파일 재사용도 통과한다.
- 실제 ArcFace 완료 source의 calibration **160,408 query / gallery 3,000**, test **182,159 query / gallery 3,000** 복원·lineage 검증 통과.
- 실제 GTX 1080 Ti, torch **2.7.1+cu118**, CUDA runtime **11.8**에서 calibration 64장의 Grad-CAM·High/Low/Random과 test 64장의 기존 특징 재사용 완료. 생성 CSV/manifest 소비 및 identity-cluster CI 재검산 통과. 제한 coverage와 High−Random 조건으로 정상 차단되었다. 임시 디렉터리만 사용했고 정식 결과로 보존하지 않았다. 이 소량 결과로 전체 faithfulness 결론을 내리지 않는다.
- 새 Jupyter 커널에서 02 **15개 셀** 순차 실행 통과. 이 검증은 in-memory 설정에서 BUILD/RUN/WRITE=False인 진단 경로이며, 원본 노트북/실험 결과를 저장하지 않았다.
- Ruff와 `git diff --check` 통과.
- 전체 calibration Grad-CAM 생성, 10,000장 정식 faithfulness, gate 통과 후 실제 saliency 보정/20-seed 성능 실험은 **미실행**이다. AdaFace/MagFace/EdgeFace의 새 생성 경로는 실제 GPU로 검증하지 않았다. 각 source/checkpoint 검증을 그대로 적용한다.
- Gate는 정답 점수에 대한 확증이나 threshold 전이 보장이 아니다. CI는 고정 codec/gallery/threshold 조건의 불확실성이며, calibration 적합·gate 선택·다중 비교를 포함한 최종 확증과 독립 test는 별도 과제다.

## 16:28 KST — Windows 진행 기록 교체 실패 및 기존 자료 복구

- 상태: **구현됨 / 실제 중단 자료 복구·기본 설정 새 커널 Run All 검증됨**.
- 기준: `c6fda99`의 02 실행. 저장된 traceback은 `PermissionError: [WinError 5] progress.tmp -> progress.json`이며 test 진행 108,032행 이후 `_atomic_json`의 단발 `os.replace`에서 중단되었다. 접근 거부를 일으킨 구체적인 잠금 프로세스는 확인하지 못했다. 모델 추론 또는 FIQA 계산 오류로 판정하지 않는다.
- 원본 UID: `saliency-inputs-492103c73c999bb998628401`. Commit된 journal은 calibration 1,254개(160,408행 전부), test 844개(108,032행). Pending journal에는 검증 가능한 test shard 1개가 추가되어 있다.

### 결정과 이전 구조 보완

1. 기존 Grad-CAM artifact 모듈의 `_replace_with_retry`를 JSON 및 Parquet publish에 재사용한다. PermissionError에 최대 8회, 지수 backoff(총 대기 최대 6.35초)를 적용한다. 영구 실패는 예외를 유지하며 기존 파일을 삭제하거나 비원자적 덮어쓰기로 우회하지 않는다.
2. `resume_from`을 추가한다. 노트북 첫 셀 `SALIENCY_RESUME_DIR`에 위 ArcFace 경로를 명시했다. 다른 모델은 None이다. 자동 최신 artifact 탐색은 하지 않는다. 생성 설정을 바꿔 새 실험을 수행할 경우 None으로 초기화한다.
3. Producer 수정에 따라 새 content UID를 사용한다. 원본의 manifest/spec/hash를 새 코드인 것처럼 바꾸지 않고, 검증된 shard를 새 UID로 복사한다. 원본 디렉터리는 변경하지 않는다. 복구 원본 경로·journal SHA·spec SHA를 새 journal과 최종 입력 manifest에 남긴다.
4. 구버전 호환은 `4a1fdf7ba3f527efaa8708dc78ecb798e4fcc36db865b3d467dc90870cdbf0a8` producer에 한정한다. Target/features 및 source 복원·표본 선택·top1·Grad-CAM·test 재사용 함수의 AST hash가 검토한 구버전과 동일해야 한다. Condition/frame/설정/runtime/다른 구현 hash가 바뀌면 복구를 거부한다. 과학적 계산 변경을 임의로 허용하는 일반적인 hash 무시 기능이 아니다.
5. Pending `progress.tmp`는 committed journal의 동일 spec·정확한 prefix 확장인 경우에만 사용한다. 모든 receipt의 순서·bytes·SHA를 검증하고, 복사 시 query ID·정렬 hash·split 및 복사본 SHA를 확인한다. 잘린 JSON은 committed journal로 돌아가고, 유효하지만 상충하는 journal은 실패시킨다.
6. 임계값, 표본 seed, 평가 정의, saliency gate는 변경하지 않는다. 00/01 및 완료 원본 run도 수정하지 않는다. 노트북 설정은 유지하고 실패한 생성 셀의 과거 진행 로그·traceback만 비워 재실행하도록 했다.

### 검증 및 재실행

- 신규 포함 관련 테스트 **74개 통과**. 기존 01 기본 설정을 False로 고정하는 검사 1개는 사용자 실행 설정과 충돌하여 제외했다. Ruff 통과.
- 일시 잠금 후 성공, 영구 잠금의 8회 종료·기존 JSON 및 pending 보존, pending shard까지 복구, GPU 생성 함수 재호출 없음, 원본 파일 hash 불변, 설정/과학 코드 불일치 거부를 합성 입력에서 확인했다.
- 실제 ArcFace 복구는 새 커널에서 BUILD=True, 비교 RUN/WRITE=False로 수행하여 자료 생성·검증 경로만 확인한다. 단일/20-seed 성능 비교를 자동으로 확증하지 않는다.
- 사용자는 노트북을 디스크 변경 내용으로 다시 연 뒤 **Kernel Restart → Run All**한다. 두 번째 커널에서 같은 출력 디렉터리에 동시에 실행하지 않는다. 잠금이 지속되면 원본을 지우지 말고 명시적 예외를 확인한다.

### 16:33 KST — 실제 전체 자료 복구 및 추가 정밀도 오류 수정

- 새 입력 `saliency-inputs-a9b8252b75742667b08f67d8`에 calibration 160,408행, test 182,159행의 특징과 calibration 가림 실험 10,000행을 완료 저장했다. Calibration GPU 재추론 없이 기존 1,254 shard 전부를 복구했고, test는 pending receipt까지 복구한 후 나머지를 기존 test 특징에서 재사용했다.
- 그 뒤 소비 단계에서 `faithfulness paired rows are inconsistent`가 추가 발생했다. Gain은 float32 masked score 차이, drop은 float64 origin score와의 차이여서 두 산술 경로의 반올림이 다르다. 실제 최대 차이는 High−Low 약 `1.49e-8`, High−Random 약 `2.98e-8`이며 기존 고정 `atol=1e-9`가 정상 행을 거부했다.
- `saliency_incremental_calibration.py`의 **행 단위 산술 일관성 검사만** float32 상대 epsilon과 CSV round-trip 절대 오차 `1e-12`를 허용한다. 저장 값, 요약 평균·CI 재검산, High−Low/High−Random gate, FPIR/TPIR 계산을 변경하지 않는다. Float32 정상 반올림 허용 및 `1e-5`의 실제 gain 변조 거부 회귀를 추가했다.
- 실제 전체 입력의 CSV/SHA/identity-cluster CI 재검산 통과. Calibration/test coverage 각각 **1.0**, faithfulness와 test sample ID 중복 **0**이다.
- 실제 gate는 `blocked`다. 이유는 데이터 부족이나 코드 예외가 아니라 High−Random 효과가 양수가 아니기 때문이다: 평균 **−0.020933817**, 95% identity-cluster CI **[−0.023208784, −0.018582412]**. High−Low 평균 **0.059452518**, CI **[0.057194872, 0.061541905]**이다. 기준을 약화하거나 성능 비교를 강행하지 않는다.
- 첫 실제 복구 실행은 자료 생성·저장까지 성공 후 위 추가 예외를 발견한 실행이며, 성공한 Run All로 계산하지 않는다. 수정 후 별도 새 커널에서 기본 설정 Run All을 검증한다. 기존 원본 progress JSON/tmp hash는 변경되지 않았다.

### 최종 검증

- 수정 후 **기본 BUILD/RUN/WRITE=True 설정 그대로 새 커널의 15개 셀 Run All 정상 종료**를 확인했다. 완료 입력을 SHA 검증 후 재사용했고, 위 실제 faithfulness 결과에 따라 단일·20-seed 비교는 정상적으로 blocked되었다. 이 실행은 비교 성공이나 saliency 개선의 증거가 아니다.
- 추가 정밀도 회귀를 포함하여 **75개 테스트 통과, 기존 01 설정 검사 1개 제외**. Ruff 및 diff whitespace 검사 통과.
- 원본 committed/pending journal SHA가 각각 `765f481a9093e3af149a3505791afe4c4526a6071f7d4814c0e176393d67d553`, `e39b3ee9a5edce229bbe7e2696190a3a3d7fe4b89850ab148b86b84a7664a056`로 그대로 유지됨을 확인했다. 새 완료 manifest의 출력 5개도 bytes/SHA 검증 통과했다.
- 실제 계산 결과는 새 입력 디렉터리에 저장되어 있다. 검증용 커널의 notebook 출력은 원본 ipynb에 덮어쓰지 않았다. 사용자가 Run All하면 완료 자료를 읽어 같은 검증 결과를 표시한다.

## 16:56 KST — 사용자 연구 목적에 맞춰 faithfulness를 비차단 진단으로 분리

- 상태: **구현됨 / 회귀 및 실제 입력의 단일·복수 split 실행 검증됨**.
- 변경 배경: 사용자는 강한 faithfulness가 확인되지 않더라도 saliency 유래 특징이 Continuous FIQA에 추가적인 보정 정보를 제공하는지 검증하고자 한다. 기존 faithfulness 필수 gate는 이 성능 비교를 막아 연구 질문에 답하지 못했다.
- 앞 절 및 20260916의 **faithfulness 통과 필수 조건을 폐기**한다. 자료 무결성·누락·lineage·누출 차단과 metric 계약은 유지한다. 이 절과 현재 02 소비 모듈이 실행 정책의 기준이다. 과거 실패한 가림 실험 값이나 과거 artifact는 변경하지 않는다.

### 결정·모듈 책임·데이터 흐름

1. `assess_incremental_gate`는 실제 자료 검증 오류만 `reasons`에 넣어 실행을 차단한다. High−Low/High−Random의 기존 판정은 그대로 계산하여 `faithfulness_status=passed/failed`, `strong_faithfulness_pass`, `warnings`와 상세 원인으로 별도 반환한다. `faithfulness_policy=diagnostic_only`, `faithfulness_required_for_comparison=False`이며 임의 검증 무시 옵션은 추가하지 않는다.
2. 파일/SHA, 동일 condition/model/gallery, alignment, calibration/test 전체 coverage, calibration 가림 근거의 ID·identity·content·split, test 비중복, raw/summary/CI 정합성, smoke-only 제한은 계속 검사한다. 통계적 기준 미충족과 잘못된 자료는 구분한다.
3. 노트북은 자료 검증과 faithfulness를 다른 행에 표시한다. 자료가 유효하면 faithfulness가 failed여도 Continuous FIQA, +outside, +entropy, +both를 적합·평가한다. 이어서 설정된 20개 fit/safety 분할도 실행한다. 최종 표에는 faithfulness failed와 성능 비교 computed가 함께 나타날 수 있다.
4. Score drop/High/Low/Random은 여전히 예측 입력이 아니다. 입력 특징·fit/safety 경계·TPIR genuine-score-topk-v2·FPIR/TPIR CI와 seed 정의는 바꾸지 않는다.
5. 완료 saliency/faithfulness 입력은 재사용한다. `saliency_calibration_inputs.py`와 생성 UID는 이번 정책 수정에서 바꾸지 않아 Grad-CAM·가림 실험 재생성은 필요 없다.

### 저장·보고 계약과 해석

- 결과 manifest만 schema **2**로 올리고 `execution_contract=data-integrity-required-faithfulness-diagnostic-v1`을 기록한다. `metric_contract=genuine-score-topk-v2`는 그대로다. 이전 정책 결과와 새 결과는 구현/manifest hash가 달라 별도 UID에 저장된다.
- `method_summary.csv`, `paired_comparisons.csv`, `models.csv`, `split_summary.csv`에 공통 saliency 진단의 `faithfulness_status`, `strong_faithfulness_pass`, `faithfulness_policy`를 포함한다. Baseline 행의 failed를 FIQA 자체의 성능 실패로 해석하지 않는다.
- 신규 `faithfulness_diagnostics.csv`는 원본 요약의 mean/CI/표본 수와 진단 상태를 담는다. Manifest의 gate에는 실패 사유가 포함된다. CSV 한 개만 읽더라도 진단 실패를 감추지 않는다.
- 성능 개선이 관찰되어도 faithfulness 또는 압축오차의 인과효과를 입증한 것으로 해석하지 않는다. `interpretation`에 탐색적 추가 예측 가치 분석이라는 범위와 위 두 주장 false를 기록한다. 실제 FPIR과 목표 충족·paired CI·분할 안정성을 함께 해석한다. 01 test 결과를 참고한 기준 선택 및 사후 정책 변경의 탐색적 성격도 유지한다.
- 02 노트북의 과거 blocked 출력은 정책 변경과 혼동되지 않도록 비웠다. 기존 실험 파일·staged 변경·00/01·기본 압축 실험·DB는 수정하지 않는다.

### 검증과 재실행

- 관련 **76개 테스트 통과**(기존 01의 사용자 실행 설정과 충돌하는 기본값 검사 1개 제외). High−Random 실패 및 High−Low 실패 각각에서 4개 방법·2개 split 실행/저장, failed 상태·진단 CSV·manifest 보존, 추가적인 특징 누락 시 차단을 검증했다. 기존 hash/lineage/CI 변조·test 변경 시 fitted model 불변 테스트도 유지된다.
- 실제 검증은 ArcFace 완료 입력 전체(calibration 160,408/test 182,159)와 10,000장 가림 근거를 재사용한다. 새 커널에서 CI 100회·분할 2개·성능 결과 저장 False로 02를 순차 실행한다. 이는 실행 검증이며 정식 CI 2,000회·20개 분할 결과가 아니다.
- 사용자는 **커널 재시작 → Run All**하면 된다. SOURCE_MODEL=arcface, BASELINE_METHOD=continuous_fiqa, 기존 생성 재사용 및 RUN/WRITE=True, BOOTSTRAP_RESAMPLES=2000, SPLIT_SEEDS=0..18/8972는 유지한다. 실패를 숨기거나 gate 통과를 위해 표본/seed를 바꿀 필요가 없다.
- 실제 새 커널 smoke 종료 확인: faithfulness failed와 comparison_enabled=True를 동시에 유지하며, 4개 방법×5개 target의 단일 분할 20행 및 두 분할 40행을 계산했다. 기존 15개 셀과 검증 assertion 셀이 모두 통과했다. 성능 결과·검증용 notebook 출력은 저장하지 않았으며 최종 2,000회 CI·20-seed 실험은 사용자 재실행 대상이다. Ruff와 diff 검사도 통과했다.

## 18:37 KST — Step12 Saliency Incremental Calibration 실험 종료 판정

- 상태: **검증됨 / 실험 종료 판정**.
- 기준: step12, HEAD `b5636c2c175f96a3321aa53e27f595c28b243d1f`.
- 변경 배경: 정식 20-split 실행 결과(`saliency-incremental-1a4d5126eb5ba82a349bd16f`)와 단일 split 결과(`saliency-incremental-097831a5f6f492f086e2caa6`)를 분석한 결과, saliency 요약 특징(outside_face_attention, saliency_entropy)의 additive calibration 효과가 없음이 확인됨.
- 이전 결정과의 관계: 16:56 KST의 faithfulness 비차단 진단 정책 유지. 02 노트북의 실행 결과를 보존하고 saliency를 calibration 예측 변수로 승격하는 방향은 종료.

### 결정

1. **Saliency incremental calibration 종료**: 20-split 전체에서 유의미한 TPIR 개선이 관찰되지 않았다. FPIR=0.10에서 +outside TPIR delta CI [-0.000499, +0.000389], +entropy [-0.000350, 0.000], +both [-0.000581, +0.000382]로 모두 0을 포함한다. FPIR=0.01의 +3.49%는 절대값 0.00005이며 다른 operating point에서 재현되지 않는다.
2. **Grad-CAM faithfulness 실패 확정**: High-Random mean -0.0209, CI [-0.0232, -0.0186]. Strong faithfulness 불충족.
3. **Saliency의 논문 내 역할**: compression sensitivity 설명의 보조 분석 도구로만 유지. Calibration 예측 변수로의 승격 불가. 논문에서는 negative result 1개 섹션으로 포함하여 연구의 정직성 확보.
4. **P0 필수 후속**: Origin Float32 대조군 FIQA 보정 및 동일 Realized FPIR 비교 곡선. 기존 score 배열 재사용, GPU 추론 불필요.
5. **02 실험 artifact 보존**: 기존 saliency_incremental 및 saliency_incremental_split_stability 결과는 변경하지 않는다. novelty/19.md에 종료 분석 기록.

### 영향받는 문서/코드

- `novelty/19.md`: 신규 작성 (saliency 종료 분석)
- `publish/step12_saliency_분석검증_보고서_20260917_v1.md`: 신규 작성 (GPT 분석 교차 검증 보고서)
- 코드 변경 없음. 기존 02 노트북, saliency 모듈, 실험 결과 불변.

### 미해결 위험

- P0 Origin Float32 대조군이 없으면 "FIQA가 압축 환경에서 도움이 된다"는 주장의 근거가 불완전하다.
- 동일 Realized FPIR 비교 곡선이 없으면 FIQA vs Global TPIR 비교의 공정성을 보장할 수 없다.
- 저FPIR(1%) calibration transfer 실패는 saliency와 무관한 구조적 문제이며 별도 해결이 필요하다.
