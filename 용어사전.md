# 얼굴 임베딩 압축 실험 용어사전

작성일: 2026-07-29  
대상: LFW·QMUL-SurvFace 얼굴 임베딩 압축, PostgreSQL/pgvector 검색,
Open-set 평가 및 Grad-CAM 노트북

이 문서는 노트북 제목, 실행 로그, 설정 및 결과 artifact에서 반복되는 영어
용어를 한국어로 설명한다. 로그 문장은 단순 번역이 아니라 해당 단계에서
실제로 수행하는 작업을 기준으로 해석한다.

## 1. 실행 단계와 로그

| 영어 용어·문장 | 한국어 의미 | 실험에서 수행하는 작업 |
|---|---|---|
| `scan` | 순차 검사·선별 | 전체 데이터를 읽으면서 특정 split·role에 해당하는 표본이나 통계를 수집한다. 보통 아직 모델을 학습하거나 DB 결과를 생성하지 않는다. |
| `fit` | 학습·적합 | development 데이터에서 PCA 투영축, PQ codebook, threshold 또는 정규화 통계를 계산한다. |
| `transform` | 학습된 변환 적용 | 이미 학습된 PCA/PQ를 다른 임베딩에 적용한다. 공식 test 데이터에는 fit하지 않고 transform만 수행한다. |
| `materialization` | 결과 실체화·저장 | 메모리에서 계산한 벡터·코드·마스크를 실제 NPY/CSV/JSON 파일이나 DB 행으로 생성한다. |
| `extraction` | 특징 추출 | 이미지 또는 aligned crop을 모델에 입력하여 임베딩이나 Grad-CAM을 계산한다. |
| `characterization` | 특성 분석 | 압축 전후의 오차, 각도, 점수, 순위, 저장량 및 검색 성능을 측정한다. |
| `validation` | 유효성 검증 | shape, dtype, hash, 표본 순서, coverage, 통계 또는 결과 계약을 검사한다. |
| `freeze` | 입력·설정 동결 | 데이터 순서, 설정, 모델 checkpoint 및 hash를 현재 run의 변경 불가능한 입력으로 기록한다. |
| `commit` | DB 저장 확정 | 현재 transaction의 DB 변경을 영구 반영한다. |
| `flush` | 버퍼 내용을 파일에 기록 | ledger 등의 메모리 버퍼를 디스크에 기록한다. DB commit과는 다른 작업이다. |
| `batch` | 묶음 처리 단위 | GPU 추론이나 DB 저장을 한 번에 처리하는 표본 묶음이다. |
| `checkpoint` | 재시작 기준점 | 중단 후 이미 완료된 작업을 확인하고 건너뛸 수 있게 저장한 상태 또는 artifact다. 얼굴인식 모델 가중치 파일도 checkpoint라고 부르므로 문맥을 구분한다. |
| `preflight` | 실행 전 사전 점검 | 파일, 설정, GPU, DB, run 및 acknowledgement가 올바른지 실제 장시간 작업 전에 검사한다. |
| `dry run` / `review mode` | 검토 모드 | 실제 DB 쓰기나 장시간 계산 없이 설정과 입력만 확인한다. |
| `reused` | 기존 완료 결과 재사용 | 동일 run에서 이미 완료되고 hash 검증을 통과한 artifact를 다시 계산하지 않고 사용한다. |
| `fail-closed` | 이상 시 즉시 중단 | 필수 입력·hash·GPU provider·좌표 등이 올바르지 않으면 불완전한 결과를 계속 사용하지 않고 중단한다. |
| `fallback` | 대체 경로 | 주 경로가 실패했을 때 다른 모델·CPU·원본 벡터 등을 사용하는 처리다. 정식 실험에서는 암묵적 fallback을 금지한다. |
| `fallback-free` | 대체 결과 미사용 | 압축 결과가 없거나 실패했을 때 원본 임베딩으로 몰래 대체하지 않은 상태다. |

## 2. 노트북 로그 문장

| 로그 문장 | 의미 |
|---|---|
| `SurvFace aligned crops` | SurvFace face crop을 모델 입력 규격으로 만드는 전처리 단계 |
| `aligned crop materialization` | RGB uint8 NHWC 112×112 배열과 aligned index를 파일로 생성하는 중 |
| `SurvFace 106-point landmarks` | SurvFace 얼굴에서 106개 랜드마크를 예측하는 단계 |
| `106-point landmark materialization` | 랜드마크 좌표와 얼굴 부위 mask용 artifact를 저장하는 중 |
| `protocol freeze` | 공식 gallery/probe 순서, config, 데이터 및 모델 hash를 run에 기록하는 단계 |
| `ArcFace extraction` | aligned 얼굴에서 ArcFace 512차원 임베딩을 추출하는 단계 |
| `batch DB commit/ledger flush 완료` | 현재 batch의 임베딩 DB 저장과 처리 이력 CSV 기록이 완료됨 |
| `SurvFace development embedding scan` | 전체 origin 임베딩을 읽으면서 training-development 표본만 선별하는 중 |
| `SurvFace compressor fit` | development 임베딩으로 PCA/PQ 압축기를 학습하는 중 |
| `SurvFace compressor fit reused` | 완료된 동일 run의 PCA/PQ 압축기를 hash 검증 후 재사용함 |
| `compressed materialization` | 학습된 압축기를 전체 origin 임베딩에 적용하고 DB·CSV에 저장하는 단계 |
| `development normalization scan` | development 표본의 압축 재구성 오차 평균·표준편차를 계산하는 중 |
| `frozen normalization accepted` | 이전에 development에서 계산해 동결한 오차 정규화 통계를 재사용함 |
| `compressed embedding batch committed` | 현재 batch의 PCA-256 벡터와 PQ 보조 코드가 DB에 저장됨 |
| `PCA sweep development normalization scan` | 여러 PCA 차원의 재구성 오차 기준을 development 데이터에서 계산하는 중 |
| `PCA sweep embedding batch committed` | 현재 batch의 여러 PCA 차원 벡터가 각 DB 테이블에 저장됨 |
| `compressed materialization reused` | 완료된 압축 DB 결과와 측정 artifact를 재사용함 |
| `Step 1 characterization` | 압축 오차·각도·점수·순위·저장 효율을 분석하는 단계 |
| `official search matrix` | 압축 profile과 exact/HNSW 검색 방식의 조합을 실행하는 단계 |
| `official probe search` | 공식 probe를 gallery에서 검색하는 단계 |
| `official evaluation` | 검색 결과로 TPIR, FPIR, Rank 성능과 threshold를 계산하는 단계 |
| `curve calculation` | 여러 threshold에서 TPIR–FPIR 곡선을 계산하는 단계 |
| `origin embeddings` | 압축되지 않은 원본 512차원 임베딩을 추출하는 단계 또는 결과 |
| `top-1 gallery templates` | 각 probe의 원본 임베딩과 가장 유사한 1위 gallery template을 구성하는 단계 |
| `population Grad-CAM extraction` | 선정된 전체 표본 집단의 Grad-CAM을 계산하는 단계 |
| `saliency feature validation` | 얼굴 부위별 saliency와 mask 결손·품질을 검증하는 단계 |
| `compression characterization` | 압축으로 발생한 임베딩·점수·검색 성능 변화를 분석하는 단계 |
| `saliency compression join` | 같은 표본의 saliency 특징과 압축 측정값을 결합하는 단계 |
| `representative case visualization` | 대표 성공·실패·순위 변화 사례를 그림으로 생성하는 단계 |

## 3. 진행률 로그 항목

| 항목 | 의미 |
|---|---|
| `elapsed` | 해당 reporter가 시작된 후 경과 시간 |
| `progress` | 완료된 비율 |
| `processed` | 현재까지 처리한 표본 수 |
| `scanned` | 현재까지 읽어서 검사한 표본 수 |
| `written` | 현재까지 파일 또는 DB에 기록한 표본 수 |
| `completed` | 진행률 계산에 사용하는 완료 수 |
| `total` | 해당 단계의 전체 처리 대상 수 |
| `rate` | 초당 처리한 표본 또는 작업 수 |
| `eta` | 현재 속도를 기준으로 계산한 예상 잔여 시간 |
| `matched` | scan한 표본 중 현재 조건에 부합한 수 |
| `inserted` | 새로 DB에 저장한 수 |
| `skipped` | 동일 run에 이미 존재해 건너뛴 수 |
| `failed` | 입력, 추론 또는 DB 저장에 실패한 수 |
| `aligned` | 모델 입력 규격으로 정상 변환된 이미지 수 |
| `source_vectors` | 압축의 원본이 되는 512차원 임베딩 수 |
| `development_vectors` | compressor 또는 통계 fit에 사용되는 development 임베딩 수 |
| `pca_inserted` | 새로 저장한 PCA 압축 벡터 수 |
| `pca_skipped` | 이미 존재하여 다시 저장하지 않은 PCA 벡터 수 |
| `pq_inserted` | 새로 저장한 PQ code 수 |
| `pq_skipped` | 이미 존재하여 다시 저장하지 않은 PQ code 수 |
| `profile` | 현재 처리 중인 압축 또는 모델 설정 |
| `fit_count` | 압축기 학습에 실제 사용된 표본 수 |
| `attempt=A001` | 해당 phase의 첫 번째 실행 시도 |

## 4. 데이터셋과 split

| 용어 | 의미 |
|---|---|
| `LFW` | Labeled Faces in the Wild. 얼굴 검증에 널리 사용되는 데이터셋이다. 이 프로젝트에서는 identity-disjoint open-set 실험 구성도 사용한다. |
| `QMUL-SurvFace` / `SurvFace` | 감시 영상에서 추출한 저해상도 얼굴 데이터셋이다. 배포 이미지가 이미 face crop이므로 각 파일에 얼굴 검출기를 다시 적용하지 않는다. |
| `split` | 학습·보정·평가 목적에 따라 데이터를 나눈 구간 |
| `development` | PCA/PQ와 일부 통계 또는 모델 선택을 fit하는 개발 데이터 |
| `calibration` | open-set threshold나 calibration 모델을 결정하는 데이터 |
| `test` / `official test` | 최종 평가에만 사용하는 데이터. compressor나 threshold를 fit하지 않는다. |
| `identity-disjoint` | development, calibration, test 사이에 같은 사람 identity가 겹치지 않는 분할 |
| `protocol` | gallery 구성, probe 역할, 순서, 평가 방법을 정의한 실험 규칙 |
| `official protocol` | 데이터셋이 공식적으로 제공한 gallery/probe 구성과 순서 |
| `protocol_role` | 표본이 training, gallery, registered probe, unknown probe 중 어떤 역할인지 나타내는 값 |
| `protocol_index` | 공식 protocol 안에서 표본의 순서를 나타내는 번호 |
| `coverage` | 전체 protocol 표본 중 실제로 정상 처리된 비율 |
| `full coverage` | 대상 표본이 하나도 누락되지 않은 상태 |

## 5. Gallery와 probe

| 용어 | 의미 |
|---|---|
| `gallery` | 검색 대상이 되는 등록 얼굴 집합 |
| `probe` | gallery에서 신원을 검색할 질의 얼굴 |
| `registered probe` / `mated probe` | gallery에 같은 identity가 존재하는 probe |
| `unmated probe` | gallery에 같은 identity가 존재하지 않는 probe |
| `unknown-unknown probe` | gallery에 등록되지 않았고 identity label도 평가에서 알려지지 않은 unknown probe |
| `known-unknown` | 등록 identity로 알려졌지만 특정 gallery 등록에는 포함되지 않은 형태의 probe 역할. 현재 SurvFace 공식 protocol에는 없다. |
| `enrollment` | 한 identity의 gallery 이미지를 등록 template으로 구성하는 과정 |
| `template` | 한 장 또는 여러 장의 임베딩을 대표하도록 만든 검색 단위 |
| `top-1` | 검색 결과 중 유사도가 가장 높은 첫 번째 후보 |
| `candidate` | 검색 결과로 반환된 gallery 후보 |

## 6. 얼굴 전처리와 랜드마크

| 용어 | 의미 |
|---|---|
| `face detection` | 장면 이미지에서 얼굴 bounding box를 찾는 작업 |
| `face crop` | 얼굴 영역만 잘라낸 이미지 |
| `alignment` | 눈·코·입 위치를 기준으로 얼굴의 위치·회전·크기를 표준화하는 작업 |
| `aligned crop` | 얼굴인식 모델 입력 규격으로 정렬된 얼굴 이미지 |
| `official_face_crop_resize` | SurvFace의 기존 face crop을 detector 없이 112×112로 직접 resize하는 현재 전처리 정책 |
| `detect_and_align` | 얼굴 검출 후 landmark로 정렬하는 정책. 현재 LFW 경로에서 사용한다. |
| `RGB uint8 NHWC` | RGB 채널, 0–255 정수, `[표본, 높이, 너비, 채널]` 배열 구조 |
| `112×112` | 현재 ArcFace 계열 모델의 입력 이미지 크기 |
| `landmark` | 눈·코·입·윤곽 등 얼굴의 기준 좌표 |
| `106-point landmark` | InsightFace 2d106 모델이 출력하는 106개 얼굴 기준점 |
| `semantic region` | 눈, 코, 입, 볼, 턱처럼 의미를 가진 얼굴 부위 |
| `region mask` | 특정 얼굴 부위에 해당하는 픽셀을 표시한 boolean 배열 |
| `face coverage fraction` | 전체 112×112 영역 중 얼굴 mask가 차지하는 비율 |
| `missing semantic region` | 저해상도·자세 문제로 특정 얼굴 부위 mask를 만들 수 없는 상태 |
| `NaN` | 해당 값을 측정할 수 없음을 나타내는 값. 없는 부위 saliency를 0으로 가장하지 않기 위해 사용한다. |

## 7. 얼굴 임베딩과 모델

| 용어 | 의미 |
|---|---|
| `embedding` | 얼굴 이미지를 신원 비교가 가능한 고정 길이 숫자 벡터로 표현한 값 |
| `origin embedding` / `origin_512` | 압축 전의 원본 512차원 얼굴 임베딩 |
| `ArcFace` | 각도 기반 margin loss로 학습되는 얼굴인식 방법·모델 계열 |
| `model UID` | 모델 family, 구조, 학습 데이터, checkpoint, preprocessing을 구분하는 고유 식별자 |
| `checkpoint` | 학습이 끝난 모델 가중치 파일 |
| `checkpoint SHA-256` | checkpoint 파일이 변경되지 않았음을 확인하는 hash |
| `preprocess hash` | 색상 순서, 크기, 평균·표준편차 등 모델 입력 전처리 계약의 hash |
| `raw embedding` | 모델 출력의 L2 정규화 전 벡터 |
| `raw norm` | raw embedding의 L2 크기 |
| `L2 normalization` | 벡터 길이를 1로 만드는 연산 |
| `normalized embedding` | L2 norm이 1인 임베딩 |
| `cosine similarity` | 두 벡터 방향의 유사도. 값이 클수록 같은 identity일 가능성이 높다. |
| `angular distance/error` | 두 임베딩 방향 사이의 각도 또는 압축 전후 각도 변화 |
| `CUDA` | NVIDIA GPU에서 PyTorch 추론을 수행하는 실행 환경 |
| `provider` | ONNX Runtime이 연산을 실행하는 backend. 예: CUDAExecutionProvider |
| `device` | PyTorch 모델이 실행되는 장치. 예: `cuda`, `cpu` |

## 8. 압축

| 용어 | 의미 |
|---|---|
| `compression profile` | 압축 방법과 차원·설정을 하나의 비교 단위로 표현한 이름 |
| `PCA` | Principal Component Analysis. 분산이 큰 방향을 학습하여 임베딩 차원을 줄이는 방법 |
| `PCA-256` / `pca_256` | 512차원 임베딩을 256차원으로 축소한 profile |
| `PCA sweep` | PCA-384, 256, 128, 64, 32처럼 여러 차원을 반복 비교하는 실험 |
| `PQ` | Product Quantization. 벡터를 여러 부분으로 나누고 각 부분을 codebook index로 표현하는 압축 방법 |
| `codebook` | PQ가 각 부분 벡터를 대표하기 위해 학습한 중심 벡터 집합 |
| `PQ code` | 각 부분 벡터가 선택한 codebook index의 나열 |
| `m` | PQ에서 원본 벡터를 나누는 subvector 개수 |
| `nbits` | 각 PQ subvector code에 사용하는 bit 수 |
| `pq_512_m16_b8` | 512차원 원본을 16개 subvector로 나누고 각 code에 8bit를 사용하는 PQ 설정 |
| `PQ auxiliary` | 오차·저장량 분석을 위한 보조 PQ code. 현재 pgvector HNSW 검색 벡터로 사용하지 않는다. |
| `reconstruction` | 압축 표현으로부터 원래 차원과 유사한 벡터를 복원하는 작업 |
| `reconstruction error` | 원본 임베딩과 복원 임베딩의 차이 |
| `reconstruction_error_norm` | development 오차 평균·표준편차를 기준으로 표준화한 재구성 오차 |
| `error normalization` | 서로 다른 압축 profile의 오차를 비교하기 위해 development 기준으로 오차를 표준화하는 것 |
| `storage bytes per embedding` | 표본 하나의 압축 표현이 차지하는 저장 byte |
| `codebook bytes` | PQ codebook 자체가 차지하는 저장 byte |

## 9. PostgreSQL과 pgvector

| 용어 | 의미 |
|---|---|
| `PostgreSQL` | 본 실험에서 이미지 메타데이터, 임베딩, 검색 결과를 저장하는 관계형 DB |
| `pgvector` | PostgreSQL에서 벡터 저장과 거리 검색을 제공하는 확장 |
| `vector table` | 임베딩 차원별 벡터를 저장하는 DB 테이블 |
| `embedding_512` | 원본 512차원 벡터 테이블 |
| `embedding_256` | PCA-256 벡터 테이블 |
| `embedding_pq` | PQ code를 보조 artifact로 저장하는 테이블 |
| `index` | 전체 행을 직접 비교하지 않고 후보를 빠르게 찾기 위한 DB 검색 구조 |
| `HNSW` | 근사 최근접 이웃 검색을 위한 그래프 기반 인덱스 |
| `exact search` | 모든 gallery 후보와 직접 비교하는 정확 검색 |
| `approximate search` | HNSW 등으로 후보를 빠르게 찾는 근사 검색 |
| `pgvector-searchable` | 해당 profile을 pgvector 거리 연산과 인덱스로 직접 검색할 수 있는지 여부 |
| `transaction` | 함께 성공하거나 함께 취소되는 DB 작업 묶음 |
| `ledger` | 각 표본의 inserted/skipped/failed 상태와 계보를 기록한 CSV 실행 이력 |

## 10. Open-set 검색과 평가

| 용어 | 의미 |
|---|---|
| `open-set` | probe의 identity가 gallery에 없을 가능성까지 고려하는 검색 문제 |
| `closed-set` | 모든 probe identity가 gallery에 존재한다고 가정하는 검색 문제 |
| `threshold` | top-1 similarity가 등록 identity로 accept될 만큼 높은지 판단하는 기준값 |
| `threshold calibration` | 목표 FPIR 등을 만족하도록 calibration 데이터에서 threshold를 결정하는 과정 |
| `accept` | similarity가 threshold 이상이어서 등록 identity로 판단 |
| `reject` | similarity가 threshold 미만이어서 unknown으로 판단 |
| `decision flip` | 압축 때문에 accept/reject 결과가 원본과 달라진 경우 |
| `rank` | 정답 identity가 검색 결과에서 나타난 위치 |
| `rank flip` | 압축 때문에 후보 순서가 달라진 경우 |
| `score drift` | 압축 전후 similarity 점수 변화 |
| `Rank-1` | 정답이 첫 번째 후보인 성능 |
| `Rank-20` | 정답이 상위 20개 후보 안에 포함되는 성능 |
| `FPIR` | False Positive Identification Rate. unknown probe를 등록 identity로 잘못 받아들이는 비율 |
| `TPIR` | True Positive Identification Rate. 등록 probe를 올바른 identity로 받아들이는 비율 |
| `DIR` | Detection and Identification Rate. 설정에 따라 TPIR과 유사한 정식별 지표로 사용 |
| `TPIR20` | 정답이 Rank-20 안에 있고 threshold 조건도 만족한 비율 |
| `TPIR–FPIR curve` | threshold 변화에 따른 정식별률과 오인식률의 관계 곡선 |
| `AUC` | 성능 곡선 아래 면적 |
| `realized FPIR` | 선택된 threshold에서 실제 측정된 FPIR |
| `top-k` | 검색 결과로 반환할 상위 후보 수 |
| `search matrix` | compression profile × search mode 조합 전체 |

## 11. Grad-CAM과 saliency

| 용어 | 의미 |
|---|---|
| `Grad-CAM` | 모델 출력에 영향을 준 이미지 영역을 gradient와 feature map으로 계산하는 설명 방법 |
| `saliency` | 모델 판단에 상대적으로 큰 영향을 준 이미지 위치 또는 중요도 |
| `heatmap` | 위치별 saliency 값을 색이나 숫자 배열로 표현한 결과 |
| `population Grad-CAM` | 한두 사례가 아니라 실험 대상으로 선정된 표본 집단 전체에 Grad-CAM을 적용한 결과 |
| `target` | Grad-CAM이 설명하려는 모델 점수 |
| `target template` | probe와 비교하는 gallery 임베딩 또는 template |
| `top-1 gallery target` | 원본 공간에서 probe와 가장 유사한 gallery 1위 template을 Grad-CAM target으로 사용 |
| `face attention` | 전체 saliency 중 얼굴 mask 내부에 위치한 비율 |
| `outside-face attention` | 전체 saliency 중 얼굴 mask 외부에 위치한 비율 |
| `region attention` | 전체 saliency 중 특정 눈·코·입·볼·턱 mask 내부에 위치한 비율 |
| `saliency concentration` | saliency가 특정 영역에 얼마나 집중됐는지 나타내는 값 |
| `saliency entropy` | saliency가 넓게 퍼졌는지 일부 위치에 집중됐는지 나타내는 지표 |
| `left-right asymmetry` | 얼굴 좌우 saliency 분포의 차이 |
| `faithfulness` | saliency가 실제 모델 판단에 영향을 준 영역을 얼마나 충실히 설명하는지 |
| `saliency–compression association` | 얼굴 부위 saliency와 압축 오차·점수 변화 사이의 통계적 관계 |

## 12. Run과 artifact 재현성

| 용어 | 의미 |
|---|---|
| `run` | 하나의 설정·입력·모델 계보로 수행하는 전체 실험 단위 |
| `run_id` | run을 구분하는 고유 문자열. 날짜, sequence와 config hash 일부를 포함한다. |
| `phase` | run 내부의 한 실행 단계 |
| `attempt` | 같은 phase를 재시도한 횟수 |
| `artifact` | 실험에서 생성한 CSV, JSON, NPY, 모델, 그림 등의 결과 파일 |
| `manifest` | 표본 목록, 순서, hash, 설정과 artifact 정보를 기록한 구조화 파일 |
| `run manifest` | run 전체의 config, 입력, 환경, Git 상태와 진행 상태를 기록한 JSON |
| `bundle manifest` | aligned crop이나 landmark bundle의 shape, count, hash 및 생성 정책을 기록한 JSON |
| `config hash` | 실험 설정 내용으로 계산한 고유 hash |
| `content hash` / `SHA-256` | 파일 내용이 동일한지 검증하는 hash |
| `lineage` / `provenance` | 결과가 어떤 입력, 모델, 압축기와 run에서 생성됐는지 나타내는 계보 |
| `immutable` | 완료 후 기존 artifact를 수정하거나 덮어쓰지 않는 정책 |
| `_SUCCESS` | 해당 bundle이 계약에 맞게 완전히 생성됐음을 나타내는 완료 표식 |
| `full_scope_complete` | 선택된 전체 범위를 누락 없이 처리했는지 여부 |
| `full_protocol_complete` | 논문용 공식 protocol 전체를 실패 없이 완료했는지 여부 |
| `official_test_fit=false` | 공식 test 데이터로 압축기나 통계를 학습하지 않았다는 기록 |
| `active_run.json` | 후속 노트북이 사용할 현재 SurvFace run 경로를 가리키는 포인터 |

## 13. 로그 읽기 예시

다음 로그를 예로 든다.

```text
SurvFace 03 compressed materialization
| compressed embedding batch committed
| progress=30%
| processed=139008
| total=463341
| pca_inserted=139008
| pq_inserted=139008
```

의미는 다음과 같다.

1. SurvFace 전체 463,341개 origin 임베딩을 압축하는 단계다.
2. 현재까지 139,008개를 처리하여 약 30% 완료했다.
3. 각 표본의 PCA 벡터와 PQ 보조 code가 DB transaction으로 저장됐다.
4. 이 문장은 compressor를 새로 학습한다는 뜻이 아니다.
5. `failed`가 없고 최종 row count가 source count와 같아야 완료로 인정한다.

다음 로그도 구분해야 한다.

```text
development normalization scan
```

이는 임베딩 벡터를 다시 L2 정규화한다는 뜻이 아니다. development 표본에서
압축 재구성 오차의 평균과 표준편차를 계산하여, 이후 전체 표본의 압축 오차를
같은 기준으로 비교할 수 있게 만드는 단계다.

