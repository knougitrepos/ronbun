```text
[데이터셋 Manifest 준비]
  LFW / QMUL-SurvFace (핵심) + RFW/BalancedFace (조건부)
        ↓
[ExperimentScope 적용]
  MODE(dev/real) + DATA_FRACTION + SEED
  → identity-disjoint / nested subset / open-set 역할 보존
        ↓
[Aligned Crop + 106-point Landmark Bundle]
  112×112 RGB uint8 NHWC + InsightFace 2d106det.onnx
  → face / eyes / nose / mouth / cheeks / jaw / outside_face 영역
        ↓
[FR Checkpoint Embedding 추출] (원본 512D 기준선)
  ArcFace (InsightFace 구현됨) / AdaFace / MagFace (adapter 존재, 실제 checkpoint 등록 미완)
  → raw norm + L2 unit embedding + lineage(hash/전처리)
        ↓
[Saliency Target 정의 + Population Grad-CAM]
  LFW: Leave-One-Out(LOO) identity template
  SurvFace: origin top-1 official gallery template
  → Pass A (전체 embedding) → Pass B (LOO target Grad-CAM)
  → 공간 특징 (entropy, 중심, 부위별 attention, faithfulness)
        ↓
[독립 압축 적용] (PCA-only / PQ-only 분리, 연쇄 없음)
  Development split에서 fit → Calibration/Test에 동결 적용
  PCA: 384/256/128/64/32D
  PQ: 원본 512D → m×nbits code budget
        ↓
[Paired 평가 + Threshold 처리]
  원본 vs 압축: geometry (MSE, angular error, cosine drift)
               score/rank (threshold crossing, rank inversion)
               open-set (DIR/TPIR@FPIR 등)
  Threshold: 원본 고정 + 압축별 재보정 (calibration split 기준)
        ↓
[Saliency–Compression Association]
  Geometry join (sample/profile 단위, 중복 방지)
  Retrieval join (protocol / threshold policy / mated 상태 strata 분리)
        ↓
[Immutable RunStore 완료 + 공통 보고]
  runs/ 아래 lineage 보존 → notebooks/common/reports/ 표·그림
```

---
# 프로세스 흐름도

```text
데이터 manifest 및 split 생성
        ↓
ArcFace / AdaFace / MagFace로 512D 임베딩 추출
        ↓
Identity별 gallery template 생성 (mean + L2 normalize)
        ↓
원본 512D 기반 검색 수행 (cosine similarity)
        ↓
Calibration split에서 FPIR 기반 threshold 선택
        ↓
PCA / PQ 압축 모델 학습 (codebook 생성)
        ↓
┌────────────────────────────────────────────┐
│ PCA 경로                                  │
│ 512D → PCA embedding → cosine search      │
│ → inverse transform → reconstruction error │
└────────────────────────────────────────────┘
        ↓
┌────────────────────────────────────────────┐
│ PQ 경로                                   │
│ 512D → PQ encode → PQ code                │
│ → ADC search (asymmetric distance)        │
│ → optional decode → reconstruction error   │
└────────────────────────────────────────────┘
        ↓
압축 프로파일별 threshold 재보정 (calibration)
        ↓
원본 vs PCA vs PQ 검색 성능 비교
        ↓
Grad-CAM 기반 saliency map 생성
        ↓
Saliency – 압축오차 – 검색민감도 상관 분석
        ↓
최종 FPIR / DIR / Rank 변화 평가
```

---

# 요약

`ronbun/step6` 코드는 **보정(calibration)과 양자화 분석을 포함한 얼굴 검색 파이프라인**을 구현하고 있다.

코드에서 원본 512차원 특징의 판정 임계값은 calibration split을 이용하여 설정한다. 미등록 인물에 해당하는 non-mated probe의 점수를 수집한 뒤, **FPIR(False Positive Identification Rate, 오인 수락 식별률)이 목표값 이하가 되도록 하면서 올바른 식별률을 최대화하는 임계값**을 선택한다.

예를 들어 목표 FPIR이 0.01이라면 다음 조건을 만족하는 임계값을 선택한다.

```text
FPIR ≤ 1%
```

이 기능은 다음 위치에 구현되어 있다.

```text
research/calibration/rejection.py
└─ choose_threshold()
```

그리고 calibration set에서 다음 보정기에 사용된다.

* `GlobalThresholdCalibrator`
* `PerCompressionThresholdCalibrator`

---

# 1. Product Quantization 구현

PQ(Product Quantization, 곱 양자화)는 다음 파일의 Faiss 기반 `PQCompressor`를 사용한다.

```text
research/compression/profiles.py
```

임베딩은 다음 과정으로 처리된다.

```text
원본 512D 임베딩
→ PQ encode
→ PQ code
```

PQ code 검색에는 다음 함수가 사용된다.

```text
search_adc()
search_adc_with_metrics()
```

이 함수들은 ADC(Asymmetric Distance Computation, 비대칭 거리 계산)를 수행하며 다음 결과를 반환한다.

* 거리
* 검색된 gallery index
* 검색시간 측정값
* index 생성시간
* gallery code 추가시간
* 질의당 검색시간

---

# 2. PQ 복원과 압축 오차 측정

PQ code는 `decode()`를 통해 근사 임베딩으로 복원된다.

```text
PQ code
→ decode
→ 근사 512D 임베딩
```

`transform_profile()`은 `CompressionResult`를 생성하며, 원본 벡터와 복원 벡터를 비교하여 다음 지표를 계산한다.

## Reconstruction error

원본 임베딩과 PQ 복원 임베딩 사이의 평균제곱오차이다.

```text
reconstruction_error
```

## Angular error

원본 임베딩과 복원 임베딩 사이의 각도 차이이다.

```text
angular_error
```

따라서 현재 코드는 다음 비교를 수행한다.

```text
원본 이미지 임베딩 512D
↔
PQ code를 복원한 근사 임베딩 512D
```

ADC 검색 결과에서는 다음이 기록된다.

* Top-k gallery index
* ADC 거리
* index 생성 지연시간
* gallery code 추가시간
* 전체 검색 지연시간
* 질의당 검색 지연시간

---

# 3. 현재 PQ 분석에서 누락된 부분

현재 코드는 **개별 이미지 임베딩의 PQ 복원 오차**를 계산한다.

그러나 다음 비교는 수행하지 않는다.

```text
원본 identity template
↔
PQ로 압축·복원한 identity template
```

Gallery template은 여러 등록 이미지를 평균하여 생성된다. 또한 leave-one-out 방식도 사용된다.

하지만 현재 PQ 오차 분석은 주로 개별 이미지 단위로 수행된다.

즉, 다음 두 분석이 분리되어 있지 않다.

```text
개별 이미지 임베딩의 압축 오차
```

```text
실제 검색에 저장된 gallery template의 압축 오차
```

현재 코드에는 identity template을 별도로 압축한 뒤 원본 template과 비교하는 전용 함수가 없다.

이를 추가하려면 다음 중 하나를 수정할 수 있다.

```text
research/runtime/face_search_runner.py
└─ template 생성 단계
```

또는:

```text
research/evaluation/compression_characterization.py
```

추가해야 할 처리 과정은 다음과 같다.

```text
identity별 gallery 이미지
→ template 집계
→ 원본 gallery template 512D
→ 동일 PQ 모델로 encode
→ decode
→ PQ 복원 gallery template 512D
→ 원본 template과 복원 template 비교
```

측정할 지표는 다음과 같다.

* Template reconstruction MSE
* Template angular error
* Template cosine-to-origin
* Identity별 평균 압축 오차
* Template 압축 오차와 ADC 검색 실패의 관계

---

# 4. ArcFace·AdaFace·MagFace의 saliency

Saliency map은 사전학습된 얼굴인식 모델에 Grad-CAM을 적용하여 생성한다.

실험 설정 파일은 다음과 같다.

```text
step2_pytorch_gradcam.yaml
```

ArcFace, AdaFace, MagFace 실험에서 다음 설정이 활성화되어 있다.

```text
run_gradcam: true
```

각 모델에서 마지막 convolution layer를 Grad-CAM 대상 계층으로 사용한다.

그다음 `pytorch-gradcam`을 통해 이미지별 heatmap을 생성한다.

기존 결과에서 세 모델은 모두 주요 얼굴 랜드마크에 집중하는 양상을 보였다.

일반적으로 관찰된 영역은 다음과 같다.

* 눈
* 눈썹
* 코
* 콧등
* 얼굴 중심 영역

모델별 개략적인 양상은 다음과 같다.

## ArcFace

ArcFace는 눈 주변과 콧등에 비교적 강하게 집중하는 경향을 보였다.

## AdaFace

AdaFace도 ArcFace와 유사하게 눈과 얼굴 중심 영역을 중요하게 사용하는 경향을 보였다.

## MagFace

MagFace 역시 눈과 눈썹 주변에서 높은 saliency를 나타내는 경향을 보였다.

따라서 세 모델 모두 얼굴 중앙의 주요 식별 특징에 의존하는 것으로 해석할 수 있다.

단, 이러한 모델별 결과는 코드 자체에 정의된 사실이 아니라 **실행 결과에서 관찰된 실험적 결과**이다.

---

# 5. Saliency와 PQ ADC 오류의 관계

Saliency는 얼굴인식 네트워크가 인물을 식별할 때 이미지의 어느 영역을 중요하게 사용했는지를 나타낸다.

반면 ADC 검색 오류는 다음 과정에서 발생한다.

```text
원본 query 512D
↔
PQ code로 저장된 gallery template
```

즉, ADC 오류의 직접적인 원인은 gallery 벡터의 양자화 근사 오차이다.

따라서 개별 이미지의 saliency map만으로 다음을 직접 설명할 수는 없다.

> 왜 PQ ADC가 해당 probe에서 잘못된 gallery template을 검색했는가?

현재 saliency는 개별 원본 이미지에 대해 계산된다.

그러나 검색에는 leave-one-out 방식으로 집계된 identity template이 사용된다.

```text
여러 gallery 이미지
→ 평균 집계
→ identity template
→ PQ code
→ ADC 검색
```

이 때문에 다음 두 수준이 혼재될 수 있다.

```text
개별 이미지의 얼굴 영역 중요도
```

```text
집계된 identity template의 PQ 검색 안정성
```

현재 분석은 이미지 단위 saliency와 template 단위 검색 결과를 연결하므로, saliency와 ADC 사이의 관계를 직접적인 인과관계로 해석하면 안 된다.

정확한 표현은 다음과 같다.

> 개별 probe 이미지의 saliency 특성이 PQ ADC 검색의 순위 변화 또는 판정 변화와 통계적으로 연관되는지를 분석한다.

다음과 같이 표현하면 부정확하다.

> 특정 얼굴 랜드마크에 집중했기 때문에 PQ ADC 오류가 발생했다.

또한 현재 코드는 집계된 template 자체에 대한 saliency를 측정하지 않는다.

---

# 6. 연구 프로세스의 현재 구조

현재 `step6`의 주요 흐름은 다음과 같이 정리할 수 있다.

```text
데이터 manifest와 split 생성
        ↓
ArcFace/AdaFace/MagFace 원본 512D 임베딩 추출
        ↓
Identity별 gallery template 집계
        ↓
원본 512D 검색
        ↓
Calibration split에서 원본 threshold 결정
        ↓
PCA/PQ 압축 모델 학습
        ↓
압축 임베딩 및 PQ code 생성
        ↓
PCA cosine 검색 또는 PQ ADC 검색
        ↓
압축 프로파일별 threshold 재보정
        ↓
원본과 압축 검색 결과 비교
        ↓
Grad-CAM saliency 특징 결합
        ↓
Saliency–압축오차–검색민감도 연관성 분석
```

---

# 7. 전체 파이프라인의 세부 설명

## 7.1 데이터 준비

먼저 dataset manifest를 생성하고 identity가 겹치지 않도록 데이터를 분할한다.

주요 split은 다음과 같다.

* Development split
* Calibration split
* Test split

역할은 다음과 같다.

| Split       | 역할                          |
| ----------- | --------------------------- |
| Development | PCA/PQ 학습                   |
| Calibration | 목표 FPIR에 대응하는 threshold 선택  |
| Test        | 고정된 압축 모델과 threshold의 최종 평가 |

Test split을 이용해 threshold를 다시 선택해서는 안 된다.

---

## 7.2 원본 임베딩 생성

각 얼굴 이미지에 대해 ArcFace, AdaFace 또는 MagFace 모델이 512차원 임베딩을 생성한다.

```text
얼굴 이미지
→ 얼굴 검출 및 정렬
→ FR 모델
→ 원본 512D 임베딩
```

원본 임베딩은 이후 모든 비교의 기준이 된다.

---

## 7.3 Gallery template 생성

동일 identity에 여러 등록 이미지가 있는 경우 임베딩을 평균하여 identity template을 만든다.

```text
동일 identity의 등록 임베딩들
→ 평균
→ L2 정규화
→ gallery identity template
```

Leave-one-out 방식에서는 분석 대상 이미지가 자기 자신의 template 생성에 포함되지 않도록 제외한다.

---

## 7.4 원본 검색

원본 probe 임베딩과 원본 gallery template 사이의 cosine similarity를 계산한다.

```text
원본 probe 512D
↔
원본 gallery template 512D
→ cosine similarity
→ Top-k ranking
```

각 probe에서 가장 높은 gallery score를 `origin_top1_score`로 사용한다.

---

## 7.5 원본 threshold 선택

Calibration split의 non-mated probe마다 gallery에서 얻은 최고 similarity를 수집한다.

```text
Non-mated probe
→ 모든 gallery template과 비교
→ 최대 similarity 추출
```

이 점수 분포를 기준으로 목표 FPIR을 만족하는 threshold를 선택한다.

예를 들어 목표 FPIR이 10%라면:

```text
Non-mated probe 중 약 10%만 threshold를 넘도록 임계값 선택
```

코드에서는 단순 분위수뿐만 아니라 목표 FPIR 제약을 만족하는 범위에서 올바른 식별 성능을 최대화하는 방식도 사용한다.

선택된 원본 threshold는 test split에 고정 적용한다.

---

# 8. 압축 왜곡과 검색 민감도의 구분

현재 연구에서는 다음 두 종류의 분석을 분리해야 한다.

## 압축 기하학적 왜곡

하나의 임베딩 벡터가 압축 후 얼마나 변했는지를 측정한다.

주요 지표:

* Reconstruction MSE
* Angular error
* Cosine-to-origin

이는 벡터 자체의 압축 오차이다.

## 검색 민감도

압축으로 인해 검색 결과나 판정이 얼마나 변했는지를 측정한다.

주요 지표:

* Agreement with origin
* Rank flip
* Top-k identity agreement
* Threshold crossing
* DIR 변화
* FPIR 변화

벡터의 reconstruction error가 크다고 반드시 검색 결과가 바뀌는 것은 아니다.

반대로 reconstruction error가 작아도 threshold 근처에 있으면 판정이 바뀔 수 있다.

---

# 9. Saliency 연관성 분석의 의미

Saliency 특징은 다음과 같다.

* 왼쪽 눈 집중도
* 오른쪽 눈 집중도
* 코 집중도
* 입 집중도
* 왼쪽 볼 집중도
* 오른쪽 볼 집중도
* 턱 집중도
* 얼굴 외부 집중도
* Saliency entropy
* 최대 영역 집중도
* 좌우 비대칭
* Saliency 중심 위치
* Saliency 분산 정도

이 특징들을 압축오차 또는 검색민감도와 결합하여 Spearman correlation을 계산한다.

```text
Saliency feature
↔
Angular error
```

```text
Saliency feature
↔
Reconstruction MSE
```

```text
Saliency feature
↔
Agreement with origin
```

```text
Saliency feature
↔
Threshold crossing
```

이 분석은 연관성을 보여주지만 인과관계를 증명하지 않는다.

---

# 10. 개선 권고사항

## 10.1 우선순위: 높음

### Gallery template 압축 오차 추가

현재 개별 이미지 임베딩의 압축 오차뿐 아니라, 실제 ADC에 저장되는 identity template의 오차를 계산해야 한다.

추가할 지표:

* Template reconstruction MSE
* Template angular error
* Template cosine-to-origin
* Template별 ADC ranking degradation

### 이미지와 template 압축 효과 분리

결과 테이블에서 다음을 명확하게 분리해야 한다.

```text
sample-level distortion
```

```text
gallery-template-level distortion
```

```text
probe-level retrieval sensitivity
```

이를 혼합하면 saliency와 ADC 사이의 해석이 불명확해진다.

### 압축 프로파일별 FPIR 보정 검증

각 PCA 및 PQ 프로파일에서 calibration threshold를 별도로 산출하고 test split에 고정 적용해야 한다.

다음 정보를 결과에 반드시 기록해야 한다.

* Threshold source split
* Target FPIR
* Calibration FPIR
* Test FPIR
* Calibration–test FPIR 차이
* Threshold selection method

---

## 10.2 우선순위: 중간

### 원본과 압축 점수 분포 기록

프로파일별로 다음 분포를 저장해야 한다.

* Mated top-1 score
* Non-mated top-1 score
* Genuine score
* Impostor maximum score
* Top-1/top-2 margin
* Calibration–test distribution shift

### ADC 오류 사례 교차분석

다음 사례들을 별도로 분류해야 한다.

* 원본과 ADC Top-1이 동일한 경우
* ADC에서만 Rank-1이 틀린 경우
* 원본과 ADC 모두 틀린 경우
* Accept에서 reject로 변경된 경우
* Reject에서 accept로 변경된 경우

각 오류 유형에 대해 다음을 비교한다.

* Template PQ 오차
* Probe quality
* Gallery quality
* Saliency 특징
* Top-1 margin
* Local gallery density

### Identity 단위 압축 오차 집계

이미지별 오차만 보지 않고 identity별로 다음을 계산한다.

* 평균 압축 오차
* 최대 압축 오차
* Identity 내부 분산
* 등록 이미지 수
* Template 오차

---

## 10.3 우선순위: 낮음

### 로그 개선

다음 정보를 결과 파일에 명확히 기록해야 한다.

* 검색 모드
* 점수 공간
* Query 압축 여부
* Gallery 압축 여부
* Template 집계 방식
* PQ code size
* Codebook size
* ADC metric
* 원본 threshold 적용 가능 여부

### PCA와 PQ의 병렬 보고

PCA와 PQ를 같은 표에 넣더라도 검색 방식 차이를 구분해야 한다.

| 방법                | Query      | Gallery    | 점수             |
| ----------------- | ---------- | ---------- | -------------- |
| 원본                | 512D float | 512D float | Cosine         |
| PCA               | PCA float  | PCA float  | Cosine         |
| PQ reconstruction | 복원 512D    | 복원 512D    | Cosine         |
| PQ ADC            | 원본 512D    | PQ code    | Squared-L2 ADC |

---

# 11. 개발 난이도별 실행계획

## 낮은 난이도

* 원본 및 압축 score 분포 저장
* 압축 프로파일별 FPIR 곡선 작성
* Calibration/test FPIR 차이 기록
* PCA와 PQ 검색 모드 명확화
* ADC 오류 유형별 결과 분리

## 중간 난이도

* 집계된 gallery template을 PQ로 encode/decode
* Template reconstruction error 계산
* Template angular error 계산
* 개별 이미지 오차와 template 오차 비교
* Template 오차와 ADC 검색 오류 연결
* Saliency와 retrieval 지표의 분석 단위 정리

## 높은 난이도

* 압축 위험을 예측하는 보정 모델 학습
* 원본 fallback 또는 reranking 전략 추가
* 지연시간을 반복 측정하여 신뢰구간 계산
* LFW와 SurvFace 외 데이터셋으로 확장
* 모델·데이터셋 간 일반화 실험
* Saliency가 실제 예측 성능을 개선하는지 ablation 수행

---

# 최종 결론

  
