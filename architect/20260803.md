# 2026-08-03 architecture 변경 기록

## 04:24 KST — 공통 batch runner의 다중 데이터셋·파생평가·통합보고 연결

- 작성일: 2026-08-03 KST
- 상태: **구현됨·기존 완료 run 기반 쓰기 없는 통합 실행 검증됨**
- 작업 브랜치: `step6`
- 이전 결정과의 관계: `architect/20260802.md`의 “노트북은 얇은 dispatcher/report로 유지하고 실험 로직은 Python에 둔다”는 결정을 유지·보완한다.

### 변경 배경

`notebooks/common/orchestration/00_batch_experiment_runner.ipynb`는 공통 Step 4 dispatcher를 호출할 수 있었지만 한 번에 한 데이터셋만 실행했다. 또한 2026-08-02에 추가된 PCA direct/reconstruction, PQ reconstruction/ADC 파생 평가와 SurvFace faithfulness, `notebooks/common/reports/00_cross_dataset_results.ipynb` 생성은 자동으로 이어지지 않았다. 저장된 과거 실행 출력과 `EXECUTE=True`, `START_NEW_RUN=True`도 남아 있어 재실행 시 불필요한 독립 run을 만들 위험이 있었다.

### 결정 내용

1. 공통 batch notebook은 `DATASET_IDS=("lfw", "survface")`처럼 한 개 또는 여러 데이터셋을 선택하고, 하나의 검증된 FR checkpoint로 데이터셋별 plan/preflight/run을 순차 실행한다.
2. `START_NEW_RUN=False`를 기본값으로 하여 동일 plan의 완료 run을 우선 재사용한다. 실제 실행은 계속 `EXECUTE=True`와 `ACKNOWLEDGE_LOCAL_EXECUTION=True`의 이중 확인을 요구한다.
3. 완료 run 내부는 수정하지 않는다. `scripts/run_integrated_postprocessing.py`가 `COMPLETED`, run/freeze ID, fallback-free 상태를 확인하고 다음 파생 artifact를 연결한다.
   - `results/paper/<dataset>/<run_id>/search_space_v2/`
   - `results/paper/survface/<run_id>/faithfulness_v1/`
   - `results/paper/common/<model>/<selection>/`
4. 최종 report notebook은 모델명, dataset 목록, model UID, run ID를 외부 Python runner에서 주입받을 수 있다. 직접 노트북을 열어 기존 기본값과 dropdown으로 실행하는 경로도 유지한다.
5. ArcFace/AdaFace/MagFace 3-model faithfulness artifact가 없으면 해당 표만 제외하고 공통 보고서는 계속 만든다. 하나의 선택 모델 실행이 다른 두 모델의 실험을 암묵적으로 시작하지 않는다.
6. source notebook의 execution count와 output은 저장하지 않는다. 실행된 report notebook은 메모리에서만 유지하고 compact CSV·PNG·manifest만 명시적 설정에 따라 기록한다.

### 현재 구현과 목표 구조의 차이

| 구분 | 변경 전 | 변경 후 |
|---|---|---|
| dataset 선택 | `DATASET_ID` 한 개 | `DATASET_IDS` 한 개 또는 여러 개 |
| 완료 run 처리 | Step 4 결과 반환에서 종료 | 4-search-mode·faithfulness·report 순차 연결 |
| 재실행 기본값 | 새 독립 run 생성 | 동일 plan 완료 run 재사용 |
| report 선택값 | notebook 내부 상수와 dropdown | 검증된 완료 run identity를 외부 주입 가능 |
| notebook 상태 | 과거 output/execution count 포함 | output-free, restart-and-run-all 계약 |

### 데이터·모듈 흐름

```text
MODEL_NAME + DATASET_IDS + RUN_TIER
  -> checkpoint smoke validation / model_uid 고정
  -> dataset별 CommonExperimentPlan
  -> dataset별 fail-closed preflight
  -> run_common_step4_experiment
       -> active run resume 또는 completed matching run 재사용
  -> postprocess_completed_run
       -> immutable completed-run identity 검증
       -> PCA direct/reconstruction + PQ reconstruction/ADC
       -> SurvFace high/low/random faithfulness (선택)
  -> run_cross_dataset_report_notebook
       -> 검증된 model_uid/run_id 주입
       -> 00_cross_dataset_results.ipynb 메모리 실행
       -> DIR·FPIR·Storage·Latency·faithfulness compact artifact
```

### 영향 범위

- 코드: `scripts/run_integrated_postprocessing.py`
- 실행 UI: `notebooks/common/orchestration/00_batch_experiment_runner.ipynb`
- 보고 UI: `notebooks/common/reports/00_cross_dataset_results.ipynb`
- 테스트: `tests/research/test_integrated_postprocessing.py`, `tests/research/test_notebook_layout.py`
- 기존 dataset별 절차 노트북과 완료 run: 변경 없음
- DB: 변경 없음

### 재실행 범위

- 코드 연결 검증에는 기존 완료 run을 재실행할 필요가 없다.
- 새 모델·checkpoint·commit 또는 새 full 결과가 필요할 때만 batch notebook에서 새 run을 의도적으로 생성한다.
- 새 evaluator를 논문 최종 결과로 승격할 때는 clean commit에서 파생 artifact를 새 버전으로 다시 생성해야 한다.

### 검증 결과와 기준

1. 새 Python 모듈 및 notebook 계약 집중 테스트: `18 passed`.
2. 두 공통 notebook은 nbformat 검증, 모든 코드 셀 compile, execution count/output 0건을 확인했다.
3. 기존 MagFace 완료 run(LFW `20260801-R001-250c665b`, SurvFace `20260802-R001-9bccb0c1`)을 명시적으로 주입하여 report notebook을 새 커널에서 top-to-bottom 실행했다.
4. 검증 실행은 `WRITE_OUTPUTS=False`로 수행했고 `status=validated_not_written`을 반환하여 기존 common artifact를 덮어쓰지 않았다.
5. 연구 테스트 전체 결과는 `321 passed, 3 failed`이다. 실패 3건은 이번 변경 범위 밖의 기존 `database_cleanup.ipynb` 저장 output 2건과 안전 기본값 기대 불일치 1건이다. 두 변경 대상 notebook 자체는 output-free이다.

### 미해결 위험과 해석 경계

- SurvFace origin FPIR calibration transfer 실패는 이 orchestration 변경으로 해결되지 않는다. 해당 threshold-dependent 결과는 계속 descriptive-only이다.
- PQ ADC는 exhaustive code-search 평가이며 IVF-PQ 성능이나 반복 latency benchmark를 대신하지 않는다.
- `RUN_SURVFACE_FAITHFULNESS=True`는 선택한 SurvFace run의 파생 계산을 수행하므로 GPU 시간과 artifact 공간이 필요하다. 기존 검증 artifact가 있으면 재사용한다.
- 여러 데이터셋을 선택하면 순차 실행하므로 한 dataset 실패 후 다음 dataset으로 조용히 진행하지 않고 즉시 중단한다.
- 3-model faithfulness 부재를 자동으로 다른 모델 실행으로 보충하지 않는다. 모델별 checkpoint와 학습 데이터 차이를 유지한 독립 run이 필요하다.

## 05:03 KST — SurvFace 3,000-ID matched calibration 전환

- 작성일: 2026-08-03 KST
- 상태: **구현됨, protocol 단위 검증됨, 새 full 결과 미산출**
- 작업 브랜치: `step6`
- 이전 결정과의 관계:
  - `architect/20260802.md`의 SurvFace origin FPIR incident P0 판정을 유지한다.
  - 기존 200-ID/단일 enrollment calibration을 논문 주 protocol에서 폐기한다.
  - 2026-08-03 04:24의 통합 runner 결정은 유지하되, SurvFace 파생 search-space의 우선 경로를 `search_space_v3_matched_calibration`으로 올린다.

### 변경 배경

기존 origin threshold는 training-derived calibration gallery 200명과 identity당 1장 enrollment에서 산정된 반면, 공식 test는 3,000-ID multi-image mean gallery였다. MagFace 완료 run에서 target FPIR 0.10, threshold 0.686036에 대해 test origin FPIR 0.506662가 관찰됐고, matched-sweep 진단은 gallery/template 체계 불일치가 주원인임을 보였다. 따라서 calibration과 test의 watch-list 크기와 template 집계 규칙을 맞추지 않은 threshold-dependent 결과는 최종 결과로 사용할 수 없다.

### 결정 내용

1. SurvFace 공식 `training_set`의 5,319 identities만 calibration source로 사용한다.
2. seed 42의 stable SHA-256 순서로 3,000 identities를 한 번 선택한다.
3. 선택 identity별 이미지를 stable 순서로 정렬하고 `floor(n/2)`를 gallery source, 나머지를 registered probe로 둔다.
4. 선택되지 않은 2,319 identities의 모든 이미지를 non-mated probe로 둔다.
5. gallery template은 calibration과 official test 모두 identity별 embedding 평균 후 L2 정규화한다.
6. 각 non-mated probe와 전체 gallery template 사이의 최대 impostor cosine을 구하고, 이 분포만으로 target FPIR threshold를 선택한다. Mated score나 DIR은 threshold 선택에 사용하지 않는다.
7. PCA/PQ fit에는 calibration watch-list의 gallery enrollment 이미지만 사용하며 registered/non-mated와 official test는 제외한다.
8. frozen-origin과 compressed별 recalibration을 계속 분리한다. PQ reconstruction cosine과 ADC도 별도 결과로 유지한다.
9. 기존 완료 run과 결과는 수정하지 않는다. 새 evaluator 결과는 `results/paper/survface/<run_id>/search_space_v3_matched_calibration/`에 파생한다.
10. 구형 compressor fit이 존재하는 완료 run을 새 정책의 fit으로 조용히 재사용하지 않고 새 run을 요구한다.

### 검증된 실제 분할

`data/interim/survface/training_manifest.csv`에서 seed 42로 다음 수량을 확인했다.

| 역할 | identity 수 | 이미지/probe 수 |
|---|---:|---:|
| training source | 5,319 | 220,888 |
| calibration gallery source | 3,000 | 60,892 |
| calibration registered probe | 3,000 | 62,041 |
| calibration non-mated probe | 2,319 | 97,955 |
| official test gallery | 3,000 | 60,294 |
| official test registered probe | 3,000 | 60,423 |
| official test unknown probe | 121,736 identities | 121,736 |

이는 gallery identity 수와 half-gallery/mean-template 원칙을 calibration과 test에 맞춘다. 단, training과 test의 촬영 분포 차이까지 제거하는 것은 아니므로 새 test FPIR은 실제 실행으로 확인해야 한다.

### 데이터·모듈 흐름

```text
training_manifest (5,319 IDs)
  -> stable identity selection (seed 42)
  -> selected 3,000 IDs
       -> floor(n/2) images -> mean + L2 -> calibration gallery templates
       -> remaining images -> registered calibration probes
  -> remaining 2,319 IDs/all images -> non-mated probes
  -> probe별 max impostor cosine
  -> non-mated-only threshold @ target FPIR
  -> threshold freeze

official test manifest
  -> official 3,000-ID mean + L2 gallery templates
  -> registered + unknown probes
  -> frozen threshold DIR/FPIR
  -> compressed별 recalibrated threshold DIR/FPIR (별도 정책)
```

### 영향받는 구현

- protocol: `research/protocols/open_set.py`
- threshold: `research/calibration/rejection.py`
- PCA/PQ characterization: `research/experiments/step2_compression.py`
- SurvFace compressor fit/materialization: `research/experiments/survface_compression.py`
- Step 4 contract: `research/experiments/step4_workflow.py`
- 설정: `configs/experiments/step1_embedding_compression.yaml`, `configs/experiments/step2_pytorch_gradcam.yaml`, `configs/experiments/survface_face_search.yaml`
- 절차 notebook: `notebooks/survface/02_compression/*.ipynb`, `notebooks/survface/04_gradcam/experiment/02_step2_compression_characterization.ipynb`
- 통합 파생/report: `scripts/refresh_step4_search_spaces.py`, `scripts/run_integrated_postprocessing.py`, `notebooks/common/reports/00_cross_dataset_results.ipynb`
- DB schema: 변경 없음

### 재실행 범위

- protocol/threshold 코드 검증에는 GPU 재실행이 필요 없다.
- 기존 Step 4 완료 run은 불변으로 유지하며 준비된 origin embedding에서 v3 파생 search-space를 새로 계산할 수 있다.
- 절차적 SurvFace compressor artifact는 fit population이 바뀌므로 새 run에서 다시 fit해야 한다.
- 최종 논문 수치는 ArcFace·AdaFace·MagFace 각각에서 v3를 산출하고, calibration FPIR·test FPIR·DIR·crossing을 재보고해야 한다.

### 검증 기준과 현재 결과

1. training/test identity가 겹치지 않는다.
2. 같은 seed와 입력에서 identity 및 image 역할 배정이 동일하다.
3. gallery ID 수는 calibration/test 각각 3,000이다.
4. calibration gallery source와 registered probe는 image-disjoint이고 identity-matched이다.
5. non-mated identities는 gallery identities와 겹치지 않는다.
6. threshold는 non-mated probe별 최대 점수만 사용하며 `score >= threshold`와 tie를 보존한다.
7. official test score는 threshold 선택에 사용하지 않는다.
8. 집중 계약 테스트 결과는 `53 passed`이다. 저장된 output이 남아 있는 변경 범위 밖 notebook 때문에 repository-wide output-free 검사 1건은 기존대로 실패한다.

### 미해결 위험

- 새 3,000-ID protocol의 실제 origin test FPIR은 아직 산출하지 않았다. 따라서 0.506662 문제가 해결됐다고 선언하지 않는다.
- calibration/test의 gallery 규모와 집계는 맞췄지만 training/test domain shift와 quality shift는 남는다.
- calibration non-mated 97,955 probes × 3,000 templates의 exact cosine 계산량이 크므로 CPU 메모리와 batch 크기를 기록해야 한다.
- PQ ADC는 reconstruction cosine과 별도 검색 공간이며, v3 결과가 생성되기 전 기존 reconstruction 기반 수치를 PQ code-search 성능으로 승격하지 않는다.
- 기존 `search_space_v2`와 v3를 혼합하지 않는다. report는 검증된 v3가 있을 때만 v3를 우선하고, 없으면 구형 결과임을 명시한다.
