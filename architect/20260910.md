# 2026-09-10 KST — 실행 seed 42 → 8972 전환

상태: 검증됨(코드·설정·노트북 정적 검사 및 회귀 테스트; 새 실제 실험은 미실행).
기준 commit: `fc24e330a817c2472f213dfad8d1b1f303354baf`.

## 배경과 결정

사용자 요청에 따라 실행 코드, YAML 설정, notebook source 및 관련 테스트의 seed 42를
8972로 통일했다. `architect/20260909.md`의 완료 artifact 불변 원칙은 유지한다.
과거 실험 기록·manifest·보고서·노트북 저장 출력의 42는 당시 실제 값이므로 변경하지 않는다.
숫자 42 중 seed가 아닌 테스트 데이터와 날짜·점수·이미지 데이터도 보존한다.

## 데이터·모듈 흐름과 영향

- 데이터 분할/표본 선택 → 압축 적합 → calibration/평가 → bootstrap CI의 새 기본 seed는 8972다.
- research, scripts, configs, notebooks 및 instruction.md의 실행 안내와 관련 테스트를 변경했다.
- 분할 안정성은 모든 반복을 같은 seed로 바꾸지 않는다. 새 기본 panel은 `0–18, 8972`로
  서로 다른 20개를 유지한다. 이전 완료 panel `0–17, 42, 8972`는 그대로 보존한다.
- 공통 보고와 cross-model matrix는 과거 seed 42 또는 새 seed 8972를 읽을 수 있다.
  서로 다른 bootstrap seed를 섞은 집계는 거부하며 manifest seed는 실제 입력에서 읽는다.
- DB 구조, GPU 환경, CI의 통계적 정의와 연구 novelty는 변경하지 않았다.
  기존 문서의 과거 seed 설명은 역사적 기록이며 새 실행 기본값은 이번 결정이 기준이다.

## 검증

- 전체 연구 테스트: 641 passed, 4 failed. 네 실패는 HEAD notebook을 읽도록 대체한
  read-only 재검사에서도 모두 재현했다. 기존 calibration 폴더 규약, metadata,
  쓰기 기본값 및 공통 보고의 저장된 실행 출력에 관한 실패다. 이번 요청에서 수정하지 않았다.
- 별도 핵심 검사 53개 통과: seed 기본값 AST 감사, 20개 seed 중복 방지,
  legacy/current report 입력 검증, FIQA notebook, split stability, common CI, 통합 보고.
- 변경 notebook 25개의 셀 개수·execution_count·outputs가 HEAD와 동일함을 확인했다.
- 실행 소스의 남은 42는 과거 결과 호환 검사와 비-seed 테스트 데이터뿐임을 검색 확인했다.
- `git diff --check` 통과. notebook 전체 실행은 새 실험/출력 변경을 일으킬 수 있어 수행하지 않았다.

## 재실행 범위와 위험

현재 구현은 새 실행 기본값을 전환한 것이지 기존 결과를 새 seed로 재계산한 것이 아니다.
완료 결과 조회에는 재실행이 필요 없다. 8972 조건의 새로운 수치를 원하면 변경된 단계부터
새 run/derived artifact로 실행해야 한다. split seed 변경 시 protocol·freeze·압축 적합부터
영향받을 수 있고, bootstrap만 바꾸면 저장 score로 CI 후처리만 가능하다.
기존 frozen run의 config/hash 불일치를 우회하거나 manifest seed만 수정하지 않는다.
새 seed가 정확도나 CI 신뢰성을 자동으로 높이지 않으며 이전 수치와 동일함을 보장하지 않는다.
