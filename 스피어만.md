## 스피어만 ρ(Spearman rho)란?

두 변수의 **값 자체가 아니라 순위가 함께 증가하거나 감소하는 정도**를 측정하는 순위상관계수입니다.

* **ρ ≈ +1**: 한쪽이 커질수록 다른 쪽도 커짐
* **ρ ≈ −1**: 한쪽이 커질수록 다른 쪽은 작아짐
* **ρ ≈ 0**: 일정한 순위 관계가 거의 없음

## 사용자 연구에서의 계산 흐름

```text
각 얼굴 이미지 또는 probe 수집
        ↓
Grad-CAM 살리언시 특징 추출
예: 눈 집중도, 입 집중도, 외부 집중도, 엔트로피
        ↓
동일 이미지의 압축 민감도 지표 추출
예: 재구성 오차(e_rec), 각도 오차(e_ang),
점수 변화, margin 변화
        ↓
두 변수의 값을 각각 순위로 변환
예:
눈 집중도 순위      : 1, 3, 2, 4
각도 오차 순위      : 1, 2, 3, 4
        ↓
두 순위가 얼마나 비슷하게 움직이는지 계산
        ↓
Spearman ρ 산출
        ↓
해석
ρ > 0 : 해당 영역 집중도가 높을수록 압축 오차도 증가
ρ < 0 : 해당 영역 집중도가 높을수록 압축 오차는 감소
ρ ≈ 0 : 뚜렷한 단조 관계 없음
```

### 예시

```text
눈 살리언시 비중
        ↕ Spearman ρ
압축 후 각도 오차(e_ang)
```

ρ가 `−0.35`라면, **눈 집중도가 높은 이미지일수록 각도 오차가 작아지는 경향**이 있다는 뜻입니다. 다만 이는 인과관계가 아니라 **통계적 연관성**입니다.
