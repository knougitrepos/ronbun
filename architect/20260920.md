# 2026-09-20 연구 구조 기록

## 14:50 KST — FIQA + saliency 결과의 읽기 전용 compact 노트북

- 상태: **구현됨 / 실제 두 보고서·새 Jupyter 커널·회귀 검증됨**.
- 배경: 원본 matrix CSV를 GitHub 연결 도구로 채팅에 그대로 반환하면 출력 한도를 넘는다. 저장·재시작 구조를 바꾸지 않고, 로컬 집계 결과를 작은 논리 단위로 읽도록 한다. 20260919의 immutable 원본·동일 조건 비교·seed별 결과 정책을 유지한다.
- 진입점: `notebooks/common/orchestration/01_batch_fiqa_saliency_calibration_compact.ipynb`. 첫 코드 셀에 명시적 두 report ID, 원본/출력 경로, 참고 파일 크기, 페이지 행 수, 미리보기 크기를 둔다. 기본 Run All은 입력 검증 → 새 compact 저장/검증 → 짧은 진입점 미리보기다. 기존 batch를 실행하지 않는다.
- 계산 모듈: `scripts/compact_calibration_results.py`. 기존 calibration 과학 모듈 목록에 추가되지 않도록 scripts에 두었으며, 원본 실험·checkpoint의 구현 SHA나 notebook 설정을 변경하지 않는다. GPU 추론·threshold fitting·bootstrap 재실행은 없다.
- 대상: 단일 seed 8972 보고서 `matrix-report-2e2ab02ddb1d039ef1084383`(metric 1,260행, paired 3,240행) 및 20-seed 보고서 `matrix-report-d690567c6f4f7e7ad44ef19e`(metric 25,200행, paired 64,800행). 현재 보고서 목록과 명시 목록이 다르면 갱신을 요구한다. 자동 latest 선택은 없다.
- 검증: source completed/expected jobs, 보고서 manifest·CSV SHA, 36조건/7방법/5목표 및 각 seed 전체성, 행 중복, family/source lineage, count/denominator와 FPIR/TPIR 일치, target flag, split summary 재집계, paired count/delta·CI를 확인한다. 개별 원본 job 전체를 재검증하는 기능은 아니며 원본 보고서·증거 ZIP의 출처를 유지한다.
- 집계 흐름: 보고서별로 분리 → 15행 dataset/target 핵심 표 → dataset별 method 목표 충족 현황 → 36조건 각각 metrics 35행 및 paired 90행. 단일 seed와 20-seed를 합쳐 21개 반복으로 계산하지 않는다. FPIR/TPIR를 dataset·모델·PQ 간 pooling하지 않는다.
- 정보 보존: 원본 전체 행을 집계에 사용한다. 목표 충족 seed 수, 실제 FPIR/TPIR min/median/max, 분모·성공 수 범위, 기존 CI 끝점 범위, fallback 최대 수/비율, faithfulness 상태, 모든 FIQA·saliency pair의 delta·positive/negative CI seed 수 및 두 방법 목표 충족 수를 남긴다. CI 끝점 범위는 통합 CI가 아니며 seed 간 독립성·다중 비교 보정·동일 실제 FPIR를 주장하지 않는다. 개별 seed 수치·계수·query 기록은 원본 및 ZIP에 유지한다.
- 사용자 수정 반영: 최초의 12KB 강제 제한을 폐기했다. **비교 맥락과 정보량 우선**, 32,000 bytes는 안내값이고 초과 파일도 그대로 보존한다. 기본 페이지 150행이어서 조건별 metrics/paired는 나뉘지 않는다. 노트북 미리보기만 6,000 bytes로 제한하며, 초과 파일은 내용을 잘라내는 대신 파일 안내를 출력한다.
- 출력: `results/calibration/matrix_compact/compact-5a51fbb472bfb777625df8aa`. START_HERE, METRICS, 보고서별 README/QUICK_SUMMARY, overview, 조건별 CSV, 페이지별 inventory와 manifest의 161개 파일. 최대 파일 25,269 bytes. source·옵션·구현 SHA를 포함한 UID이며 동일 UID는 SHA 검증 후 재사용한다. 원본 matrix tree 아래로 출력하는 설정은 거부한다.
- 검증 근거: 새 Python 3.11 Jupyter 커널에서 노트북 전체 실행 통과. 관련 테스트 4개 통과(전체 합성 grid/목표 실패 보존, 입력·파생 값 오류 검출, 원본 불변·재사용·출력 변조 검출, 넓은 행의 손실 없는 페이지 분할). Ruff 통과. 원본 batch notebook 및 두 보고서의 8개 파일, 총 9개 파일 SHA가 전후 동일하다.
- 재실행 범위: compact 노트북만 Kernel Restart → Run All. 전체 원본 분석을 재계산하는 대신 기존 보고서 집계만 수행한다. 새 report가 생기면 REPORT_IDS를 갱신한다. 한 번에 전체 compact 폴더를 채팅으로 출력하면 여전히 한도를 넘을 수 있으므로 진입점→선택 파일 순으로 읽는다. 정리 과정의 중간 출력은 tmp로 이동했고 원본 결과는 수정·삭제하지 않았다.
