# 2026-08-09 Step 7 architecture decisions

## 21:30 KST — EdgeFace 추가 및 RFW 진단 평가 분리

- 작성일: 2026-08-09 (KST)
- 상태: 구현 중
- 작업 브랜치: `step7`

### 변경 배경

기존 정식 비교는 ArcFace, AdaFace, MagFace 체크포인트에 한정되어 있었다.
세 체크포인트는 학습 데이터와 전처리가 완전히 통제된 loss ablation이 아니므로,
모델 계열을 넘어 반복되는 압축 경향을 확인하려면 다른 경량 구조와 학습 소스를
가진 외부 체크포인트가 필요하다. 사용자가 공식 EdgeFace-XS gamma=0.6 가중치를
`models/edgeface/edgeface_xs_gamma_06.pt`에 배치했다. 또한 RFW는 추가 데이터셋으로
사용하되 BalancedFace는 이번 단계에서 유예한다.

### 결정 내용

1. EdgeFace-XS gamma=0.6을 네 번째 checkpoint-level 비교 대상으로 추가한다.
   - family: `edgeface`
   - architecture: `edgeface_xs_gamma_06`
   - training dataset: `webface12m`
   - input: RGB 112x112, `(x - 127.5) / 127.5`
   - strict loader: official `edgenext_x_small` + recursive low-rank linear
     replacement(gamma=0.6)
   - target layer: `model.stages.3.blocks.2.convs.2`
2. MagFace 기본 체크포인트 경로를 의미가 명확한
   `models/magface/magface_ms1mv2.pth`로 변경한다. 체크포인트 내용과 lineage는
   SHA-256으로 구분하며 파일명 자체를 새로운 모델로 해석하지 않는다.
3. RFW는 공식 4-group 10-fold 1:1 verification 진단으로만 사용한다.
   - RFW를 open-set DIR/FPIR 평가로 재명명하지 않는다.
   - RFW 영상/쌍에서 PCA 또는 PQ codec을 fit하지 않는다.
   - LFW 또는 SurvFace development split에서 fit한 frozen codec을 RFW에 적용하는
     cross-dataset transfer만 허용한다.
   - 각 held-out fold의 threshold는 같은 그룹의 나머지 9개 fold에서 선택한다.
4. BalancedFace의 source inventory는 보존하되 Step 7 실행 입력, codec fit,
   threshold calibration에서 제외하고 `deferred_by_step7_scope`로 기록한다.
5. LFW 프로토콜은 현재 코드 구성 그대로 유지한다. query를 고위험/저위험으로
   나누는 uncertainty/defer 실험은 추가하지 않는다.
6. 기존의 두 실행 인터페이스를 모두 유지한다.
   - all-in-one: `notebooks/common/orchestration/00_batch_experiment_runner.ipynb`,
     `notebooks/common/reports/00_cross_dataset_results.ipynb`
   - step-by-step: `notebooks/lfw/**`, `notebooks/survface/**`
   공통 Python 모듈을 canonical 구현으로 사용하되 어느 한 인터페이스를 제거하거나
   합치지 않는다. `ARTIFACT_STORAGE_MODE`, 실행 승인, run 재사용/새 run 선택처럼
   재현성과 비용에 영향을 주는 설정 변수는 유지한다. 정말 제거해야 할 경우 해당
   노트북과 architecture 기록에 삭제 이유, 대체 경로, 재실행 영향을 먼저 남긴다.

### 현재 구현과 목표 구조의 차이

- 구현됨: EdgeFace family/adapter/backbone/strict loader/profile/runner alias,
  MagFace 경로 변경, RFW pair-scoring 평가 함수, BalancedFace 유예 계약.
- 검증 필요: 실제 EdgeFace 체크포인트 smoke test와 Grad-CAM target의 gradient,
  전체 회귀 테스트, LFW/SurvFace full experiment.
- 미구현: RFW tar/BIN에서 112x112 입력을 materialize하고 네 모델의 전체 임베딩을
  추출하는 장시간 실행 단계. 따라서 이번 변경만으로 RFW 수치 결과가 생성되지는
  않는다.

### 데이터 및 모듈 흐름

```text
EdgeFace checkpoint
  -> strict official loader
  -> common PyTorch adapter
  -> unchanged LFW/SurvFace Step 4
  -> origin/PCA/PQ metrics

RFW official pair manifest
  + origin embeddings or externally fitted frozen-codec embeddings
  -> cosine pair scores
  -> group-wise 9-fold threshold selection
  -> held-out accuracy/TAR/FAR and group gap
```

### 영향 범위

- 코드: `research/embeddings/**`, `research/experiments/pipeline_runner.py`,
  `research/evaluation/rfw_verification.py`
- 설정: `configs/experiments/step2_pytorch_gradcam.yaml`,
  `configs/experiments/rfw_balancedface_quantization.yaml`
- 실행 문서: `notebooks/common/orchestration/00_batch_experiment_runner.ipynb`,
  `notebooks/common/orchestration/README.md`
- 환경: `requirements-step2.txt`, `requirements-step2-cu118.lock.txt`
- 테스트: EdgeFace strict-load/adapter, 공통 runner/config, RFW fold 평가 및
  BalancedFace 유예 계약

### 재실행 범위

기존 ArcFace/AdaFace/MagFace 완료 run은 immutable하게 유지한다. EdgeFace는 새
`model_uid`로 LFW와 SurvFace를 각각 새 full run으로 실행해야 한다. RFW 결과는
별도 supplementary diagnostic artifact로 만들며 기존 LFW/SurvFace open-set 표에
동일 metric인 것처럼 합치지 않는다.

### 검증 기준

- EdgeFace 공식 체크포인트가 missing/unexpected key 없이 strict load된다.
- uint8 RGB 입력에서 raw 512D, raw norm, unit-normalized 512D가 유한하다.
- 선언한 Grad-CAM target layer가 존재하고 backward gradient를 받는다.
- 공통 runner가 `edge` 별칭을 EdgeFace profile과 정확한 로컬 경로로 resolve한다.
- RFW 평가에서 held-out fold가 threshold 선택에 포함되지 않는다.
- RFW summary가 `open_set_protocol=false`, `codec_fit_on_rfw=false`를 기록한다.
- LFW의 기존 protocol/config는 바뀌지 않는다.
- all-in-one 및 step-by-step 인터페이스와 사용자 설정 변수의 존재가 유지된다.

### 미해결 위험

- WebFace12M과 RFW의 identity non-overlap 증거가 없으므로 EdgeFace의 RFW 결과도
  headline 일반화 근거가 아니라 보조 진단으로 제한한다.
- 모델들의 architecture, training data, preprocessing가 다르므로 네 모델 비교는
  loss function의 인과 효과를 증명하지 않는다.
- `timm==0.9.16`을 CUDA 11.8/GTX 1080 Ti 환경에서 고정했지만 실제 full run의
  VRAM/throughput은 첫 EdgeFace run에서 기록해야 한다.
- Frozen codec transfer에서는 codec source(LFW 또는 SurvFace)를 결과 행과
  manifest에 명시해야 하며 서로 섞어 하나의 RFW 결과로 평균내지 않는다.

## 05:39 KST — RFW streaming extraction, frozen-codec transfer, codec persistence

- 작성일: 2026-08-09 (KST)
- 상태: 구현됨, 정식 GPU 실험은 미검증
- 이전 결정과의 관계: 21:30 KST 절의 EdgeFace/RFW 분리 원칙을 보완한다. LFW 프로토콜 유지, BalancedFace 유예, RFW supplementary 1:1 경계는 변경하지 않는다.

### 변경 배경

RFW pair evaluator만으로는 raw aligned BIN에서 동일 모델의 임베딩을 재현 가능하게 추출할 수 없었고, 완료된 LFW/SurvFace run은 compact metrics만 남겨 fitted PCA/PQ codec을 다른 데이터셋에 적용할 수 없었다. 또한 기존 저장량 회계는 PQ codebook은 포함하지만 PCA projection/mean은 제외해 비대칭이었다. 실험 seed도 Faiss PQ centroid 학습 seed로 명시적으로 전달되지 않았다.

### 결정 내용

1. RFW aligned BIN은 archive 전체를 이미지 폴더로 풀지 않고 공식 pair 순서에 따라 batch streaming decode한다. archive SHA-256, 네 group, group당 12,000 occurrences와 6,000 labels를 fail-closed 검증한다.
2. RFW origin embedding artifact는 model UID, checkpoint SHA, preprocessing hash, protocol hash, archive SHA, TTA 설정과 산출물 SHA를 고정하고 `_SUCCESS`가 일치할 때만 재사용한다.
3. RFW에는 LFW 또는 SurvFace에서 fit한 codec만 적용한다. codec file SHA, fit source run ID, fit manifest SHA가 모두 필요하며 RFW 평가 함수 안에서는 `fit()`을 호출하지 않는다.
4. PCA는 direct-coordinate cosine과 reconstructed cosine을 분리하고, PQ는 reconstructed cosine과 symmetric ADC negative squared-L2를 분리한다. RFW 결과에는 open-set 지표를 생성하지 않는다.
5. 다음 LFW/SurvFace compression phase부터 실제 사용한 PCA/PQ 객체를 attempt-qualified run artifact로 저장하고 `frozen_codec_manifest.json`에 source dataset/run/model/seed/SHA를 기록한다.
6. PQ experiment seed는 `faiss.IndexPQ.pq.cp.seed`에 전달한다. PCA/PQ 총저장량은 공통 `codec_parameter_bytes`를 사용하며 PCA projection+mean과 PQ codebook을 모두 합산한다.
7. 기존 all-in-one runner/report를 유지하고 RFW에는 `00_data_preparation → 01_embeddings → 02_compression` 단계별 노트북을 추가한다. `ARTIFACT_STORAGE_MODE`, 실행 승인, 쓰기/재사용 설정은 삭제하지 않는다.

### 현재 구현과 목표 구조의 차이

- 구현됨: streaming BIN loader, immutable origin artifact, frozen-codec evaluator, fold-bootstrap CI, EdgeFace report alias, codec 저장 경로, PQ seed, 대칭 storage accounting, RFW 단계별 노트북.
- 미실행: EdgeFace LFW/SurvFace full paper run, EdgeFace RFW 48,000-occurrence extraction, 새 codec manifest를 이용한 RFW transfer.
- 기존 completed run에는 새 codec artifact가 없으므로 소급 생성하지 않는다. 다음 정식 run 이후에만 RFW `CODEC_CONFIGS`를 SHA-pinned 값으로 채운다.

### 데이터 및 모듈 흐름

```text
RFW pair_protocol.csv + RFW_test.tar.gz + model registry JSON
  -> streaming aligned pair batches
  -> common PyTorch adapter
  -> immutable origin_embeddings.npy + occurrence manifest

LFW/SurvFace development embeddings
  -> seeded PCA/PQ fit
  -> attempt-qualified .joblib/.faiss + frozen_codec_manifest.json
  -> SHA-pinned FrozenCodecSpec
  -> RFW direct/reconstruction/ADC pair scores
  -> group-wise 9-fold calibration + held-out metrics + fold bootstrap CI
```

### 영향 범위

- 코드: `research/datasets/rfw_aligned_bin.py`, `research/experiments/rfw_pipeline.py`, `research/evaluation/rfw_verification.py`, compression/Step 4 workflow와 compact-summary generator.
- 설정: `configs/experiments/step2_pytorch_gradcam.yaml`, `configs/experiments/rfw_balancedface_quantization.yaml`.
- 노트북: 기존 common all-in-one 두 파일 유지, RFW embeddings/compression 단계별 두 파일 추가.
- artifact: 다음 LFW/SurvFace run부터 codec 파일과 manifest가 추가되며, 기존 completed run은 변경하지 않는다.
- DB: 이번 변경은 PostgreSQL/pgvector materialization 또는 index schema를 변경하지 않는다.

### 재실행 범위 및 검증 기준

1. 변경 코드를 clean commit으로 고정한 뒤 EdgeFace LFW와 SurvFace full run을 각각 새 run ID로 실행한다.
2. 각 run에서 model/checkpoint/preprocess lineage, CUDA device, codec manifest, PQ seed, codec artifact SHA를 확인한다.
3. RFW 전체 protocol bundle을 생성하고 EdgeFace origin embeddings를 추출한다.
4. LFW-source와 SurvFace-source codec을 섞지 않고 source별 RFW 결과를 별도로 생성한다.
5. 모든 RFW manifest에서 `open_set_protocol=false`, `fit_on_rfw=false`, held-out fold exclusion, 24,000 pair/48,000 occurrence를 검증한다.
6. PCA/PQ storage summary가 `codec_parameter_bytes`를 포함하고 compact summary schema 3에서도 역사적 artifact fallback이 유지되는지 검증한다.

### 미해결 위험

- WebFace12M과 RFW identity overlap이 검증되지 않아 EdgeFace-RFW는 supplementary diagnostic을 넘는 일반화 근거로 사용하지 않는다.
- RFW symmetric ADC는 pair verification을 위한 양방향 점수이며 PostgreSQL/pgvector 1:N latency 증거가 아니다.
- 현재 PQ latency는 exhaustive Faiss 측정이고 IVF-PQ/pgvector ANN 실험이 아니다.
- 새 codec persistence는 다음 run부터 유효하므로 기존 세 모델 결과의 RFW transfer를 만들려면 세 모델의 compression phase를 새 코드로 재실행해야 한다.

## 06:17 KST — 단일 Step 4 후처리 경로와 FPIR 10%/1% 다중 operating point

- 작성일: 2026-08-09 (KST)
- 상태: 구현됨, 정식 v4 파생 산출물 생성은 미실행
- 이전 결정과의 관계: 21:30 및 05:39 KST 절의 완료 run 불변성, LFW
  프로토콜 유지, RFW 보조 진단, BalancedFace 유예 결정을 유지하면서 Step 4
  파생 평가와 공통 보고 계약을 보완한다.

### 변경 배경

기존 정식 결과의 후처리 경로가 모델별로 달랐다. ArcFace는 구형
`generate_step4_compact_summaries.py`가 만든 `summaries`만 있었고, AdaFace와
MagFace는 `refresh_step4_search_spaces.py`가 만든 `search_space_v2` 또는
`search_space_v3_matched_calibration`을 사용했다. 동일 protocol UID만으로 원천
데이터의 유효성은 확인할 수 있지만, producer·schema·검색 공간·calibration
diagnostics가 다른 artifact를 한 논문 표에 섞으면 검증 계약이 비대칭이 된다.
또한 SurvFace의 대표 operating point가 FPIR 10%에만 집중되어 더 엄격한 1%에서의
압축 검색 보존을 함께 확인할 필요가 있었다.

### 결정 내용

1. 여섯 개 기존 ArcFace/AdaFace/MagFace × LFW/SurvFace 완료 run의 원본 artifact는
   변경하지 않는다. 동일한 `scripts/refresh_step4_search_spaces.py`만 사용해
   `results/paper/<dataset>/<run_id>/search_space_v4_multi_fpir`에 새 compact 파생
   산출물을 만든다.
2. PCA/PQ transform과 검색 score 계산은 profile·search mode별로 한 번만 수행한다.
   같은 calibration/test score ledger에서 FPIR 10%와 1% threshold를 각각 선택해
   중복 GPU embedding 추출이나 중복 search를 피한다.
3. retrieval summary grain은 `family × profile × search_mode × threshold_policy ×
   target_fpir`로 확장한다. 압축 geometry summary는 target과 무관하므로 기존
   `family × profile` grain을 유지한다.
4. v4 manifest에는 단일 producer 경로, 두 target FPIR, PCA/PQ family manifest,
   target별 origin calibration signature, compact CSV SHA-256을 기록한다. 각 family는
   `origin_calibration_diagnostics.json`을 별도 보존한다.
5. 공통 보고 노트북은 기본적으로 v4만 허용하고 선택 dataset 사이의 artifact type을
   균질하게 검증한다. v3/v2/legacy 재사용은
   `ALLOW_LEGACY_POSTPROCESSING=True`인 탐색 실행에서만 가능하며 논문 표의 기본
   경로가 아니다.
6. all-in-one runner와 step-by-step 노트북 구조, `ARTIFACT_STORAGE_MODE`, 실행 승인,
   write/reuse 설정은 유지한다. 유지보수 cleanup 노트북은 source checkout이나 DB
   reset을 기본 실행하지 않는 안전값으로 되돌리고 모든 노트북 출력은 비운다.

### 데이터 및 모듈 흐름

```text
immutable completed Step 4 run
  -> one canonical refresh_step4_search_spaces.py
  -> fit/transform/search once per PCA/PQ profile and search mode
  -> calibrate the same scores at FPIR 0.10 and 0.01
  -> PCA/PQ family diagnostics + SHA-256 manifests
  -> search_space_v4_multi_fpir compact merge
  -> homogeneous common report selection
```

### 영향 범위

- 코드: `research/experiments/step2_compression.py`,
  `scripts/generate_step4_compact_summaries.py`,
  `scripts/refresh_step4_search_spaces.py`,
  `scripts/run_integrated_postprocessing.py`
- 설정: `configs/experiments/evaluation_contract_v1.yaml`,
  `configs/experiments/step2_pytorch_gradcam.yaml`
- 노트북: common report의 v4 fail-closed 선택과 유지보수 notebook 안전 기본값;
  all-in-one/step-by-step 구조 및 사용자 실행 변수는 유지
- artifact: 기존 `summaries`, `search_space_v2`,
  `search_space_v3_matched_calibration`은 역사적 산출물로 보존하고 v4를 새 디렉터리에
  생성
- DB: PostgreSQL/pgvector schema나 index는 변경하지 않는다.

### 재실행 범위

코드와 설정을 clean commit으로 고정한 뒤 기존 여섯 완료 run마다 통합
postprocessing만 실행한다. embedding extraction, Grad-CAM, 원본 Step 4 run은 다시
실행하지 않는다. EdgeFace는 아직 완료 run이 없으므로 향후 LFW/SurvFace full run이
생긴 뒤 같은 v4 후처리를 적용한다. 공통 보고서는 선택된 모든 dataset의 v4가
준비된 이후 실행한다.

### 검증 기준

- 단일-target 호출은 기존 결과 행 수와 primary diagnostics 계약을 유지한다.
- 두 target 호출에서 score/search 계산 횟수는 증가하지 않고 retrieval 행만 target
  수에 따라 증가한다.
- 모든 v4 retrieval 행은 target FPIR가 정확히 `{0.10, 0.01}`이고 중복 grain이 없다.
- PCA/PQ origin calibration signature가 target별로 일치한다.
- 공통 보고서는 v4 family manifest와 diagnostics SHA-256을 확인하며 v4/legacy
  혼합을 거부한다.
- 노트북은 output-free이고 all-in-one/step-by-step 구조 및
  `ARTIFACT_STORAGE_MODE`를 유지한다.

### 미해결 위험 및 명시적 유예

- FPIR 1%의 held-out realized FPIR가 목표를 초과하면 해당 target의 threshold-dependent
  주장은 manifest의 `validation_required` 제한을 따른다. 단순히 calibration 목표를
  추가한 것만으로 성능 보장을 주장하지 않는다.
- EdgeFace–RFW identity overlap은 확인되지 않았으므로 RFW 결과는 strict unseen
  identity 일반화 증거가 아니라 supplementary diagnostic이다.
- Faiss IVF-PQ operational ANN baseline은 후속 연구로 유예한다.
- PostgreSQL/pgvector IVFFlat도 실행 시간과 연구 범위 확대 위험 때문에 유예한다.
  현재 latency는 exhaustive search의 단일 관측치일 뿐 안정적인 DB benchmark가 아니다.

## 08:43 KST — RFW all-in-one/절차적 실행과 공통 supplementary 보고 완성

- 작성일: 2026-08-09 (KST)
- 상태: 구현됨, preflight 및 notebook validation-copy 검증됨
- 이전 결정과의 관계: RFW를 supplementary 1:1 verification으로 제한하고 RFW에서
  codec을 fit하지 않는 21:30/05:39 KST 결정을 유지한다. 06:17 KST의 LFW/SurvFace
  v4 open-set 보고와는 metric·table을 분리한다.

### 변경 배경

기존 RFW 노트북은 protocol과 EdgeFace 48,000 occurrence origin embedding까지
실행됐으나 마지막 frozen-codec 노트북이 빈 수동 `CODEC_CONFIGS`에서 즉시
실패했다. 공통 orchestration/report도 RFW를 실행하거나 별도 검증·보존하는 경로가
없었다. 또한 MagFace checkpoint 파일명을 `magface_ms1mv2.pth`로 바꾼 뒤 registry의
이전 경로와 JSON 직렬화 공백 차이 때문에 `EXECUTE=False` 공통 runner preflight도
`FileExistsError`로 중단됐다.

### 결정 내용

1. `notebooks/rfw/00_rfw_all_in_one.ipynb`를 추가한다. 한 노트북에서 공식 protocol,
   origin embedding, 명시적 frozen-codec lineage, RFW 10-fold 평가를 계획·실행·재개한다.
   기본값은 실행하지 않는 preflight이며 `EXECUTE`, 사용자 승인, 개별 stage flag가
   모두 필요하다.
2. 기존 `00_data_preparation → 01_embeddings → 02_compression` 노트북은 절차적
   단일-stage runbook으로 유지한다. 실행 변수와 `ARTIFACT_STORAGE_MODE`를 삭제하지
   않는다.
3. 수동 artifact path/SHA 사전 대신 사용자가 `CODEC_SOURCE_RUN_DIRS`로 완료 run을
   명시한다. canonical resolver는 완료 marker, run/freeze/codec manifest, dataset,
   run ID, model UID, artifact SHA와 byte count를 검증한다. 최신 run 자동 선택은 없다.
4. codec source model UID는 RFW origin model UID와 반드시 같아야 한다. 다른 모델의
   512D codec이 차원만 같다는 이유로 적용되는 경로를 차단한다.
5. codec manifest가 없는 역사적 run만 존재하는 동안에도 origin-only RFW baseline은
   고유 evaluation UID 아래 완료할 수 있다. 이 결과의 `codec_count=0`은 압축
   일반화 근거가 아니다.
6. 공통 runner는 `RUN_RFW_VERIFICATION=True`일 때 같은 실행에서 완료된 LFW/SurvFace
   run의 codec만 선택해 RFW supplementary 평가를 만든다. 공통 report는 검증된 RFW
   profile/group CSV를 별도 section과 별도 출력으로 보존하며 open-set join에는
   포함하지 않는다.
7. model registry의 MagFace checkpoint 경로를 새 파일명으로 맞춘다. registry 재사용은
   JSON text formatting이 아니라 parsed manifest의 의미적 동일성을 비교하되, 내용이
   다른 manifest는 계속 overwrite하지 않는다.

### 데이터 및 모듈 흐름

```text
RFW JPG protocol + aligned BIN
  -> validated 24,000 pair protocol
  -> same-model origin embeddings (48,000 occurrences)
  -> explicit completed LFW/SurvFace run
  -> frozen_codec_manifest + codec SHA/model UID verification
  -> origin / PCA direct+reconstruction / PQ reconstruction+symmetric ADC
  -> RFW profile and group supplementary tables

LFW/SurvFace v4 open-set tables --------------------> common report open-set section
RFW 1:1 verification tables ------------------------> separate supplementary section
```

### 영향 범위

- 코드: `research/experiments/rfw_pipeline.py`, `research/experiments/__init__.py`,
  `research/embeddings/manifests.py`, `scripts/run_integrated_postprocessing.py`
- 노트북: RFW all-in-one 신규 추가, RFW 세 단계 runbook 보완, common runner/report의
  선택적 RFW supplementary 경로
- registry: MagFace 동일 SHA checkpoint의 현재 로컬 경로로 갱신
- artifact: 기존 `results/rfw_step7/origin_embeddings/edgeface-...`는 읽기 전용으로
  보존; 새 RFW evaluation은 origin/codec/bootstrap contract UID별 immutable directory
- DB: PostgreSQL/pgvector schema 및 index는 변경하지 않는다.

### 재실행 범위와 검증 기준

- 기존 EdgeFace RFW origin embedding은 manifest/SHA가 일치하므로 재추출할 필요가 없다.
- origin-only RFW baseline은 codec 없이 실행 가능하다.
- 압축 RFW 결과에는 같은 EdgeFace model UID로 새 코드에서 완료되어 codec manifest를
  가진 LFW 또는 SurvFace run이 필요하다.
- all-in-one notebook은 기본 preflight 설정으로 top-to-bottom 실행돼야 한다.
- common runner는 validation copy에서 `EXECUTE=False`로 top-to-bottom 실행돼야 한다.
- 모든 수정 notebook은 AST-valid, output-free, restart-and-run-all 계약을 만족한다.
- 공통 report manifest는 RFW 포함 여부, model UID, `open_set_protocol=false`,
  `codec_fit_on_rfw=false`와 source manifest SHA를 기록한다.

### 남은 제한

- 현재 새 codec manifest를 가진 EdgeFace LFW/SurvFace 완료 run은 없으므로 압축 RFW
  수치 자체는 아직 생성되지 않았다.
- EdgeFace–RFW identity overlap은 미확인이다. RFW는 architecture-independent 또는
  strict unseen-identity headline 근거가 아니다.
- RFW evaluator는 verification accuracy, TAR/FAR와 group/fold bootstrap을 보고한다.
  EER 또는 지정 FAR의 TAR를 실제 구현·검증하기 전에는 노트북 결과로 기재하지 않는다.

## 10:17 KST — RFW-Custom 1:N, 다중 FPIR CI 및 cross-dataset calibration transfer 분리

- 작성일: 2026-08-09 (KST)
- 상태: 구현됨(코드·focused test), full checkpoint 실험 및 논문 수치 검증 전
- 이전 결정과의 관계:
  - 21:30/05:39/08:43 KST의 **RFW-Official 1:1 전용 경계와 RFW에서 Official용
    codec을 fit하지 않는 결정은 유지**한다.
  - 다만 “RFW를 오직 Official 1:1로만 사용한다”는 데이터셋 전체 범위는 부분
    보완한다. Official과 UID/artifact가 다른 **RFW-Custom 비공식 1:N**을 새로 둔다.
  - 06:17 KST의 FPIR 10%/1% score-reuse 결정을 RFW-Custom과 CI/appendix까지
    확장한다.
  - 08:43 KST에 미구현으로 기록한 EER는 held-out fold score/label 기반 empirical
    EER로 구현했다. 기존 other-9-fold Accuracy/TAR/FAR threshold의 의미는 바꾸지
    않으며 새 full artifact 생성 전까지 수치 검증 상태로 승격하지 않는다.

### 변경 배경

LFW와 SurvFace만으로는 controlled/surveillance 두 domain의 open-set 압축 결과만
비교할 수 있었다. RFW identity/image metadata에는 demographic group이 있으므로
Official pair/fold를 재명명하지 않고 별도의 identity-disjoint gallery/probe protocol을
구성하면 cross-domain open-set diagnostic을 추가할 수 있다. 동시에 FPIR 1%를
강화하고 source calibration을 고정해 여러 target에 적용하는 실험은 protocol kind,
score statistic, physical population과 identity overlap을 명시하지 않으면 1:N max-score와
1:1 pair-score를 잘못 섞을 위험이 있다.

### 결정 내용

1. RFW source parser의 이미지 행을 사용해 `rfw-custom-identity-balanced-open-set-v1`
   protocol family를 구성한다. Official positive/negative pair와 fold는 사용하지 않는다.
2. 동일 `source_identity_id`가 여러 demographic group에 나타나면 split 전에 전부
   제외한다. 나머지를 group별 deterministic development/calibration/test identity로
   나누고 test를 gallery, registered, known-unknown, unknown-unknown으로 분할한다.
3. 모든 RFW-Custom 행과 manifest는 `official_result_eligible=false`,
   `checkpoint_overlap_status=UNKNOWN`, `strict_unseen_identity_evidence=false`를
   기록한다. quick은 group × role을 보존하는 nested identity subset이다.
4. 공통 Step 4 runner는 LFW, SurvFace, RFW-Custom에 동일한 Origin, PCA
   direct/reconstruction, PQ reconstruction/exhaustive ADC, Grad-CAM,
   saliency×compression, compact 흐름을 적용한다. 모델 비교 단위는 ArcFace,
   AdaFace, MagFace, EdgeFace의 개별 pretrained checkpoint다.
5. source run의 PCA/PQ 검색 점수는 profile/search mode당 한 번만 계산하고 FPIR
   `0.10`, `0.01` threshold를 재적용한다. `frozen_origin`과
   `recalibrated_compressed`를 분리한다.
6. compact retrieval 행은 origin/compressed false accept 수, non-mated 분모,
   realized FPIR, DIR/FPIR Wilson 95% CI, compressed-minus-origin DIR/FPIR와
   paired probe bootstrap 95% CI(2,000회, seed 42)를 기록한다. CI는 probe-level이며
   identity-cluster 또는 checkpoint-training uncertainty가 아니다.
7. 공통 report는 세 open-set dataset을 선택 가능하게 하고 LFW/SurvFace FPIR
   10%·1% appendix CSV를 별도 생성한다. RFW-Custom은 open-set 표에, RFW-Official
   TAR/FAR/EER는 별도 supplementary 표에 둔다.
8. `cross_dataset_calibration_transfer.ipynb`는
   `CALIBRATION_SOURCE_DATASET_IDS`와 target `DATASET_IDS`를 독립적으로 받는다.
   score/protocol/codec SHA와 model UID가 일치하는 경우에만 frozen threshold를
   적용한다.
9. strict external transfer는 서로 다른 `physical_dataset_id`와 pairwise
   `disjoint_verified` identity audit를 요구한다. 같은 RFW population의
   Custom→Official 또는 overlap `UNKNOWN`은 명시적
   `same_domain_overlap_diagnostic`으로만 허용한다.
10. 1:N `maximum_gallery_score` FPIR threshold와 1:1 `pair_score` FAR threshold는
    호환되지 않는다. RFW-Official internal other-9-fold baseline과 external fixed
    threshold diagnostic도 서로 다른 evaluation mode로 기록한다.
11. Faiss IVF-PQ, pgvector IVFFlat, ANN parameter sweep, BalancedFace,
    uncertainty/defer 및 고위험/저위험 query 실험은 Step 7 이후로 유예한다.

### 현재 구현과 목표 구조의 차이

- 구현됨: RFW-Custom protocol/quick subset/immutable bundle, aligned BIN dedup
  materializer, 공통 Step 4 adapter, 다중 FPIR·CI compact schema, RFW-Official EER,
  protocol-aware transfer API, runner/report/transfer runbook.
- 코드 수준 검증됨: synthetic protocol leakage/lineage, 실제 RFW manifest 40,607행
  audit와 40,520행 custom partition, focused regression tests, notebook AST/metadata.
- 미검증: 네 checkpoint × 세 open-set dataset full GPU run, 생성된 RFW-Custom
  Grad-CAM/compact paper artifact, 실제 source×target calibration artifact matrix,
  새 EER 필드를 가진 RFW-Official full frozen-codec artifact.

### 데이터·모듈 흐름

```text
RFW Official image manifest (pair/fold labels unused)
  -> cross-group source-identity exclusion
  -> group-balanced identity-disjoint dev/cal/test
  -> RFW-Custom roles + UNKNOWN overlap boundary
  -> official aligned BIN unique-image materialization
  -> checkpoint origin embeddings + Grad-CAM
  -> development-only PCA/PQ fit
  -> calibration-only FPIR 10%/1% thresholds
  -> custom test 1:N score reuse
  -> frozen-origin / recalibrated-compressed
  -> saliency joins + compact CI artifacts
  -> common open-set report

RFW Official pairs + externally fitted frozen codec
  -> group-wise other-9-fold Accuracy/TAR/FAR
  -> held-out-score empirical EER
  -> separate supplementary tables

frozen calibration source(s)
  -> protocol/score/codec/population/overlap compatibility gate
  -> selected DATASET_IDS
  -> strict external transfer OR named same-domain diagnostic
```

### 영향받는 코드·설정·노트북·DB·artifact

- dataset/preprocessing: `research/datasets/rfw_custom.py`,
  `research/experiments/rfw_custom_pipeline.py`,
  `research/experiments/step4_datasets.py`
- evaluation/orchestration: `research/experiments/step2_compression.py`,
  `research/experiments/step4_workflow.py`,
  `research/experiments/cross_dataset_calibration.py`,
  `research/evaluation/metrics.py`, `research/evaluation/rfw_verification.py`,
  `research/experiments/rfw_pipeline.py`
- compact/postprocessing: `scripts/generate_step4_compact_summaries.py`,
  `scripts/refresh_step4_search_spaces.py`,
  `scripts/run_integrated_postprocessing.py`
- 설정: `configs/experiments/step2_pytorch_gradcam.yaml`,
  `configs/experiments/evaluation_contract_v1.yaml`,
  `configs/experiments/rfw_balancedface_quantization.yaml`
- 노트북: common batch runner/report 및 신규
  `common/orchestration/cross_dataset_calibration_transfer.ipynb`; 기존 RFW 전용
  노트북은 Official 경로로 유지한다.
- artifact: 기존 completed run은 불변이다. 새 RFW-Custom run, v4 multi-FPIR
  compact, transfer evaluation, EER 포함 RFW-Official artifact는 새 UID/디렉터리다.
- DB: PostgreSQL/pgvector schema와 index는 변경하지 않는다.

### 재실행 범위

1. 기존 LFW/SurvFace 완료 run의 immutable population을 새 CI/다중 FPIR compact로
   refresh하거나, 동일 clean commit에서 full run을 새로 수행한다.
2. RFW-Custom은 aligned unique-image bundle과 landmark prerequisite를 확인한 뒤 각
   checkpoint별 full Step 4 run을 처음부터 수행한다.
3. 공통 report는 선택한 모든 dataset/model run이 동일 v4 producer/schema를 가진 뒤
   실행한다.
4. RFW-Official EER는 기존 완료 evaluation을 덮어쓰지 않고 새 evaluation UID로
   생성한다.
5. transfer notebook은 완료 calibration/score/codec/overlap artifact를 명시적으로
   주입한 validation copy에서 먼저 실행한다.

### 검증 기준

- RFW-Custom dev/cal/test identity 교집합과 gallery/non-gallery 교집합이 비어 있다.
- 각 demographic group과 모든 protocol role이 full/quick에 존재하고 gallery와
  registered identity 집합이 같다.
- RFW-Custom source archive/protocol SHA와 aligned unique image coverage가 일치한다.
- 모든 retrieval grain은 target FPIR `{0.10, 0.01}`을 정확히 포함하고 score-search
  호출 수는 target 수 증가로 늘지 않는다.
- false accept/분모가 realized FPIR와 일치하고 rate가 Wilson interval 안에 있으며
  compressed-minus-origin과 paired CI 열이 완전하다.
- report는 LFW/SurvFace appendix 두 operating point를 모두 확인하고 RFW Official과
  Custom 표를 결합하지 않는다.
- transfer는 incompatible 1:N/1:1 score, codec/model/SHA mismatch, 동일 physical
  population 또는 UNKNOWN overlap의 strict external 사용을 fail-closed로 거부한다.
- RFW Official EER provenance는 held-out score/label이고 Accuracy/TAR/FAR의
  other-9-fold threshold와 동일 threshold라고 기록하지 않는다.
- 모든 notebook은 output-free, AST-valid, `restart_kernel_and_run_all` metadata를
  만족한다.

### 미해결 위험

- EdgeFace를 포함한 checkpoint training identity와 RFW overlap은 `UNKNOWN`이다.
  RFW 결과는 strict unseen-identity, architecture-independent 또는 causal loss 근거가
  아니다.
- RFW-Custom은 동일 RFW source population에서 만든 비공식 protocol이므로 Official
  benchmark 수치와 직접 비교하거나 “RFW official open-set”으로 부르면 안 된다.
- probe-level CI는 identity correlation과 checkpoint 재학습 변동을 반영하지 않는다.
- FPIR 1%에서는 calibration/test non-mated 수가 작으면 Wilson CI가 넓고 realized
  FPIR가 목표를 초과할 수 있다. 그런 행은 threshold-dependent claim을 보류한다.
- 실제 cross-dataset transfer 조합 중 protocol kind, score statistic, gallery size,
  score space 또는 codec이 다른 조합은 설계상 평가되지 않는다.
- full GPU run과 실제 transfer artifact matrix가 없으므로 현재 상태는 구현 완료이지
  논문 결과 검증 완료가 아니다.

## 10:34 KST — RFW demographic compact와 checkpoint-overlap 계약 보강

- 작성일: 2026-08-09 (KST)
- 상태: 구현됨, 코드 회귀 검증됨; full GPU/논문 수치 검증은 미실시
- 이전 결정과의 관계: 10:17 KST의 RFW-Custom/transfer 결정을 보완하며 폐기하지 않는다.

### 변경 배경과 결정

RFW-Custom manifest에 demographic group이 존재하더라도 retrieval row와 compact 결과에서
group을 잃으면 `cross-domain open-set + demographic` 역할을 실제 분석 artifact로 수행할 수
없다. 따라서 RFW-Custom query마다 `rfw_group`을 image lineage로 fail-closed 결합하고,
group별 DIR/FPIR·Wilson CI·paired origin delta CI를
`rfw_custom_demographic_summary.csv`로 저장한다. 이 표는 RFW-Official groupwise 1:1 표와
protocol UID 및 metric 체계를 공유하지 않는다.

또한 calibration source-target identity overlap과 checkpoint-training-target identity overlap은
서로 다른 불확실성이다. `ProtocolScoreContract`에
`checkpoint_training_identity_overlap_status`를 필수화하고 artifact manifest와 평가 summary에서
검증한다. EdgeFace–RFW는 `UNKNOWN`으로 기록되며
`strict_unseen_identity_evidence=false`가 자동 산출된다. 서로 다른 physical dataset 및
pairwise `disjoint_verified` audit가 있더라도 이 checkpoint 경계는 별도로 유지한다.

### 데이터·모듈 흐름과 영향 범위

```text
RFW-Custom test query image_id
  -> selected manifest rfw_group lookup (1 image : 1 group 검증)
  -> retrieval row + UNKNOWN checkpoint boundary
  -> group별 open-set rate/count/CI 집계
  -> artifacts/step2_workflow/rfw_custom_demographic_summary.csv

calibration source/target population overlap audit
  + target checkpoint-training overlap status
  -> cross-dataset transfer summary
  -> strict external eligibility와 strict unseen-identity 해석을 분리
```

- 코드: `research/experiments/step2_compression.py`,
  `research/experiments/step4_workflow.py`,
  `research/experiments/cross_dataset_calibration.py`
- 설정: `configs/experiments/step2_pytorch_gradcam.yaml`
- 노트북: `notebooks/common/orchestration/cross_dataset_calibration_transfer.ipynb`
- DB와 ANN index: 변경 없음

### 재실행 범위와 검증 기준

- 새 demographic compact를 얻으려면 RFW-Custom compression phase 또는 동일 population 기반
  v4 refresh를 새 artifact 경로에서 실행해야 한다. 기존 completed run은 변경하지 않는다.
- group별 summary에는 African, Asian, Caucasian, Indian이 모두 존재해야 하고 각 row의
  `checkpoint_training_identity_overlap_status=UNKNOWN`,
  `strict_unseen_identity_evidence=false`를 검증한다.
- transfer score manifest가 checkpoint-overlap 필드를 누락하거나 값이 다르면 평가 전에
  중단한다.
- 연구 회귀 테스트 394개가 통과했고, 신규 transfer notebook은 write/execute 기본값을 끈
  상태로 top-to-bottom 실행하여 error 0을 확인했다.

### 미해결 위험

- demographic CI도 probe-level이므로 동일 identity 내 상관과 checkpoint 불확실성을 반영하지
  않는다.
- 실제 4 checkpoint × 3 open-set dataset full run 및 RFW-Official full artifact가 없으므로
  논문 표의 수치적 결론은 아직 검증되지 않았다.
- Faiss IVF-PQ, pgvector IVFFlat, ANN sweep, BalancedFace,
  uncertainty/defer와 고위험·저위험 query 실험은 기존 결정대로 유예한다.

## 10:52 KST — 명시적 4-checkpoint × 3-open-set 결과 matrix

- 작성일: 2026-08-09 (KST)
- 상태: 구현됨, 합성 artifact 회귀 검증됨; 실제 12-run matrix 미실행
- 이전 결정과의 관계: 10:17/10:34 KST의 “checkpoint-level 비교”와 3개 open-set
  dataset 결정을 보완한다. 기존 단일-checkpoint 상세 보고 경로는 유지한다.

### 변경 배경과 결정

기존 공통 보고서는 한 번에 한 checkpoint의 dataset 간 상세 비교를 검증했다. 이는 실행과
lineage 격리에는 적합하지만, 요청된 ArcFace·AdaFace·MagFace·EdgeFace ×
LFW·SurvFace·RFW-Custom 12칸 통합 비교를 직접 만들지는 못했다. 따라서 최신 run을 자동
탐색하지 않고 사용자가 명시한 12개 completed-run directory만 받는 별도 matrix loader를
추가한다. 4개 model 또는 3개 dataset 중 하나라도 빠지면 전체 matrix를 거부한다.

각 run에 대해 `COMPLETED`, run/freeze identity, fallback-free, checkpoint SHA,
prepared-population lineage, v4 summary/family manifest, compact CSV byte 수와 SHA-256,
FPIR `{0.10, 0.01}`, PCA/PQ search mode, threshold policy, count/rate/CI 계약을 검증한 뒤
compression/retrieval/joined table을 결합한다. 결과의 `matrix_uid`는 12개 source의 content
identity로 결정한다. model family 간 결과는 architecture/loss 인과 비교가 아니라
pretrained-checkpoint generalization으로만 기록한다.

### 흐름·영향 범위

```text
explicit MODEL_RUN_MATRIX (4 models x 3 datasets)
  -> 12 completed/fallback-free source runs
  -> v4 compact manifest + output SHA verification
  -> FPIR/CI/search-mode contract verification
  -> compression_summary + retrieval_summary + joined_summary
  -> deterministic matrix_uid
  -> atomic compact matrix artifact
```

- canonical 코드: `research/experiments/cross_model_matrix.py`
- report 실행 연결: `scripts/run_integrated_postprocessing.py`
- 노트북: `notebooks/common/orchestration/00_batch_experiment_runner.ipynb`,
  `notebooks/common/reports/00_cross_dataset_results.ipynb`
- artifact: `results/paper/common/four_model_three_dataset/<matrix_uid>`
- DB/ANN index: 변경 없음

### 재실행·검증 기준과 미해결 위험

- checkpoint별 batch run과 v4 refresh를 먼저 완료한 후 12개 run directory를 모두 명시한다.
  자동 latest 선택이나 일부 matrix의 암묵적 사용은 허용하지 않는다.
- 합성된 12-run artifact에 대한 loader/writer, SHA tamper, incomplete matrix 및 통계 계약
  테스트 6개를 통과했다. 관련 notebook/postprocessing/pipeline focused test는 43개를
  통과했다.
- 실제 4×3 full GPU run이 아직 없으므로 matrix 수치와 논문 결론은 검증되지 않았다.
- dataset protocol 난이도가 서로 다르므로 matrix가 생겨도 dataset 간 절대 DIR/FPIR을 같은
  모집단 성능처럼 해석하지 않는다.

## 11:06 KST — same-domain diagnostic 우회 범위 명확화

- 작성일: 2026-08-09 (KST)
- 상태: 구현됨, focused test 및 notebook 안전 실행 검증됨
- 이전 결정과의 관계: 10:17 KST의 transfer gate 설명 중 `UNKNOWN`을 same-domain과 함께
  표현한 부분을 좁혀 수정한다.

`allow_same_domain_diagnostic=True`는 동일 `physical_dataset_id` 또는 동일
`source_population_uid`인 경우에만 사용한다. 서로 다른 물리 dataset 사이의 identity-overlap
audit가 `UNKNOWN`이라는 이유만으로 이 옵션을 사용해 strict external gate를 우회할 수 없다.
그 경우 `disjoint_verified` audit가 없으면 평가 전에 중단한다. 따라서 RFW-Custom →
RFW-Official처럼 동일 RFW source population인 비교만 명시적 same-domain diagnostic이 되며,
LFW → RFW처럼 물리 dataset이 다른 조합의 `UNKNOWN` audit는 허용되지 않는다.
