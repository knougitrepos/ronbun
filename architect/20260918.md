# 2026-09-18 연구 구조 기록

## 07:10 KST — 02의 saliency 무효 query를 FIQA-only로 처리

- 상태: **검증됨(구현·단위·회귀·실제 입력 축소 실행)**. 정식 20개 분할/CI 2000회 성능 결과 산출은 사용자 Run All 대상이다.
- 기준: `step12`, HEAD `1393038074b6a410368578aa0281d5b4ebd8e0c1` + 작업 트리 변경.
- 배경: 사용자는 saliency가 유효한 query는 FIQA+saliency, 무효한 query는 FIQA-only로 처리하여 AdaFace/MagFace/EdgeFace 비교를 계속하도록 요청했다. 기존 100% 수치 coverage 요구는 일부 무효 heatmap 때문에 전체 성능 비교를 차단했다.

### 이전 결정과의 관계

- 20260916 및 20260917의 **모든 query에 유효 saliency 수치가 있어야 한다는 조건을 변경**한다. 모든 query 행의 존재·정렬·출처 검증은 유지한다.
- 20260917 16:56의 faithfulness 진단 전용 정책은 유지한다. Failed를 passed로 바꾸거나 가림 실험을 threshold 입력으로 쓰지 않는다.
- 20260917 18:37 및 `novelty/19.md`의 saliency 방향 종료 결정과 달리, 최신 사용자 요청에 따라 **차단되었던 추가 모델 비교를 한정적으로 재개**한다. 현재 실행 정책의 기준은 이 문서와 새 코드다. 이전 ArcFace 결과와 문서는 덮어쓰지 않는다. 통계적 개선 미확증을 일반적인 효과 없음의 증명으로 확장하지 않는다.
- 논문 전체 범위나 `THESIS_RESEARCH_PLAN.md`를 새 성능 기여가 확정된 것으로 바꾸지 않는다. 새 정책의 정식 결과는 아직 산출하지 않았다.

### 결정·데이터 흐름·모듈 책임

1. 명시적 완료 source/condition/FIQA/saliency 입력 → hash/lineage/전체 query 무결성 검증 → 유효 saliency mask → 동일 identity 기반 fit/safety → 혼합 threshold → 원래 test 전체 평가 순서다. Test 결과로 seed·경로·threshold를 고르지 않는다.
2. 새 `research/calibration/saliency_fallback.py`에서 두 saliency 특징의 공통 유효 mask를 사용한다. 명시적으로 unavailable/invalid인 heatmap만 FIQA-only로 처리한다. 행 누락, null flag, 무한대, valid로 표시된 비유한 특징, 파일 손상·출처 불일치는 계속 차단한다. NaN을 0으로 채우거나 query를 삭제하지 않는다.
3. FIQA-only 계수는 전체 fit non-mated에서, saliency 계수는 같은 분할의 유효 fit non-mated에서만 학습한다. Saliency fit 표본이 20개 미만이면 해당 방법 전체를 FIQA-only로 처리하고 사유를 남긴다.
4. 두 경로의 원시 threshold를 조합한 후 **전체 safety non-mated에 하나의 공통 offset**을 적용한다. FIQA-only fallback은 독립 baseline의 최종 threshold와 반드시 같지는 않다(혼합 방법의 safety offset이 적용됨). 각 경로별 FPIR 보장 또는 test FPIR 보장이 아니다.
5. `research/calibration/continuous.py`의 기존 fit 및 safety 산술을 private helper로 분리해 공유한다. 01의 public API·fit/safety 정의·평가 정의는 유지한다. 공통 파일 hash 변경으로 향후 새 01 실행의 UID는 달라질 수 있지만 기존 01 결과 재실행은 이번 작업의 필수 조건이 아니다.
6. `research/experiments/saliency_incremental_calibration.py`는 유효/무효 경로별 counts를 저장하고 전체 test에서 canonical genuine-score-topk-v2로 평가한다. Calibration cache에는 genuine score/rank가 없으므로 경로 진단에서는 acceptance와 FPIR만 계산한다. Calibration TPIR/count는 빈 값, `tpir_available=False`다. Test TPIR는 정답 점수와 rank로 계산한다.
7. `02_saliency_incremental_threshold_calibration.ipynb`는 설정·실행·표시 역할을 유지한다. 현재 사용자 설정 EdgeFace/L, continuous_fiqa, BUILD/RUN/WRITE=True, CI 2000회, seed 0..18/8972는 바꾸지 않는다. 과거 저장 출력은 보존하고 변경 전 기록임을 명시한다.

### Artifact 및 재실행 경계

- 결과 schema는 **3**, 실행 계약은 `data-integrity-required-faithfulness-diagnostic-fiqa-fallback-v2`, 정책은 `invalid-saliency-fiqa-only-joint-safety-v1`이다. Metric contract는 **genuine-score-topk-v2 그대로**다.
- 기존 표에 fallback count/fraction 및 faithfulness 진단을 보존한다. 새 `fallback_diagnostics.csv`는 fit/safety/test의 경로별 분모·오수락·FPIR 및 test 정답 식별 수·TPIR를 담는다. `fallback_queries.csv`는 입력 단계에서 무효한 saliency query 목록이다. 유효 fit 부족으로 전체 fallback인 경우는 model 설정과 경로 counts로 확인한다.
- 새 UID에 저장하며 과거 결과를 고치지 않는다. 입력 producer/UID 계약은 변경하지 않아 설정·환경이 같으면 완료 saliency/faithfulness 묶음을 hash 검증 후 재사용한다.
- 모델별 `SOURCE_MODEL`만 변경 → 커널 재시작 → Run All. 현재 경로 설정 세 개(`SALIENCY_RESUME_DIR`, `SALIENCY_FEATURE_DIR`, `FAITHFULNESS_DIR`)는 None을 유지한다. 기본 BUILD=True가 완료 입력을 재사용한다.
- 성능 적합/CI는 새 정책으로 다시 계산한다. 이전 optimizer 중간 iteration이나 split 중간부터 이어지는 checkpoint 기능을 추가한 것은 아니다. 같은 완료 결과 UID의 저장 재호출은 검증 후 재사용한다.
- 00/01 노트북, 기본 batch runner, FR/FIQA 추론, source run, DB/pgvector 및 CUDA 환경은 변경하지 않는다. ArcFace도 새 정책과 동일 코드 계약으로 네 모델을 통합 비교하려면 02를 다시 실행한다.

### 검증 기준과 현재 결과

- 관련 테스트 **84개 통과, 1개 제외**. 제외는 기존 01 노트북의 실행 플래그를 False로 강제하는 검사로, 이미 True인 사용자 설정과 충돌한다. 01 설정을 변경해 통과시키지 않았다.
- 무효 saliency의 전체 cohort 유지, 혼합 safety 목표, test score 변경 시 학습 불변, 유효 fit만 스케일 적합, 모델 JSON 왕복, all-valid의 기존 saliency 수치 일치, all-invalid의 FIQA 수치 일치, 손상/누락 차단, 실패 faithfulness 보존, 결과 저장·재사용을 검사했다.
- 중단 전 실제 자료 검증에서 calibration cache에 없는 `true_identity_score`를 요구하던 새 경로 진단 오류를 발견했다. Calibration FPIR-only 진단으로 수정하고 실제 cache와 같은 열 구성의 회귀를 추가했다. 이 오류 수정 전 작업을 완료된 것으로 간주하지 않는다.
- Ruff 및 `git diff --check` 통과. Jupyter notebook 스킬의 재현성 검증 방식에 따라 저장된 원본 출력은 덮어쓰지 않고 in-memory 사본을 새 커널에서 실행한다.
- 공통 continuous fitter의 HEAD 버전과 변경 버전을 합성 calibration에서 직접 대조했다. FIQA-only/margin/margin+distortion/runner-up × 5개 목표, 총 20개 모델의 직렬화 값과 예측 threshold가 정확히 일치했다. 기존 노트북 15개 셀의 schema/코드 AST와 저장 출력·execution_count 보존도 확인했다.

### 남은 위험

- 경로별 소표본 비율은 기술통계이며 전체 paired CI를 대체하지 않는다. 특히 fallback 행이 적은 모델의 경로별 개선을 확증하지 않는다.
- Held-out empirical safety는 calibration→test 전이 보장이 아니다. 고정 threshold CI와 동일 cohort의 split 반복을 독립 실험 CI로 해석하지 않는다.
- 01 test 결과를 본 baseline 선택 및 사후 fallback 정책 추가 때문에 탐색적 비교다. 개선이 생겨도 faithfulness 또는 압축오차 인과효과를 입증하지 않는다.
- Label-free origin-top1 saliency는 원본 gallery 접근이 필요한 offline 분석이다. 압축 DB 단독 배포 구현/비용 검증은 이번 작업 범위가 아니다.

### 07:14 KST — 실제 네 모델 새 커널 실행 검증

- 모델별 새 커널에서 02의 15개 셀과 추가 assertion 셀을 순차 실행해 **네 모델 전부 통과**했다. 각 전체 calibration 160,408행, test 182,159행을 사용했다. 표본 query를 줄인 검증은 아니다.
- 실행 범위는 목표 FPIR 1%/10%, 단일 seed 8972 및 두 seed 0/8972, 성능 CI 100회다. 모델별 단일 비교 8행/분할 비교 16행이며, 실제 faithfulness 진단 failed를 그대로 보존하면서 gate=ready로 비교를 완료했다.
- 모든 방법·분할의 전체 safety non-mated 오수락률이 해당 목표 이하이고, 전체 test 분모가 유지됨을 assertion으로 확인했다. 각 saliency 추가 방법의 test fallback 수는 아래와 같았다.

| 모델 | Calibration 무효 saliency | Test FIQA-only fallback |
|---|---:|---:|
| ArcFace | 0 | 0 |
| AdaFace | 1 | 2 |
| MagFace | 5 | 11 |
| EdgeFace | 128 | 149 |

- 이 검증의 WRITE=False이며 정식 성능 결과와 원본 notebook 출력은 저장하지 않았다. 정식 5개 목표·20개 seed·CI 2000회 실험 완료나 성능 개선을 주장하지 않는다. 실제 파일 저장·동일 UID 재사용은 별도의 단위/회귀 테스트에서 검증했다.
- 별도로 AdaFace/MagFace/EdgeFace 각각 새 커널에서 **기본 BUILD=True, CI 2000회 설정의 입력 생성 셀**을 실행했다. Adapter 생성 및 chunk 추론을 호출하면 실패하도록 guard를 두어 재추론 없이 완료 입력을 재사용함을 확인했다. 모두 `reuse_completed`, gate=ready로 통과했다. 실제 CUDA preflight는 GTX 1080 Ti / torch 2.7.1+cu118 / CUDA runtime 11.8이었다.
- 재사용 UID: AdaFace `saliency-inputs-7c807016541f24e49ac2a2ef`, MagFace `saliency-inputs-3e174994b9baae468caf21fd`, EdgeFace `saliency-inputs-45dffd1416a3a8c075660f2e`. 각각 `results/calibration/saliency_inputs/<source_run_id>/` 아래의 기존 완료 묶음이며, 원본 CSV/manifest는 수정하지 않았다.
