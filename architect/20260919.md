# 2026-09-19 연구 구조 기록

## 14:59 KST — 전체 데이터셋·FR·PQ의 FIQA+saliency batch 실행

- 상태: **구현됨 / 단위·회귀 및 실제 입력 smoke 검증됨**. 정식 36조건·20분할·CI 2,000회 성능 실험은 아직 실행하지 않았다.
- 기준 commit: `65469e9cd9c5ab3400d2bf9ab722ce18069c83e6` + 이번 작업 트리.
- 사용자 요청: 기존 완료 기본 실험을 재사용하여 LFW/RFW-Custom/SurvFace × ArcFace/AdaFace/MagFace/EdgeFace × PQ m128/m64/m32의 36조건을 모두 비교한다. 특정 조건에서 이득이 있어야 나머지를 실행한다는 이전 제안은 폐기한다.

### 이전 결정과의 관계

- 20260918의 invalid saliency → FIQA-only fallback 및 공통 safety offset을 유지한다. 20260917의 faithfulness diagnostic-only 정책도 유지한다.
- 20260917–18의 SurvFace 한정 실행 범위를 세 open-set 데이터셋으로 확장한다. `THESIS_RESEARCH_PLAN.md`나 novelty 문서에 성능 기여가 확정되었다고 추가하지 않는다. 과거 종료 판단과 달리 이번 사용자 요청의 36조건 실행이 현재 추가 실험 범위다.
- 기존 calibration 00/01/02의 SurvFace 설정과 저장 출력은 보존한다. 제목부에 새 전체 batch 노트북 링크만 추가했다.

### 결정과 데이터 흐름

1. `notebooks/common/orchestration/01_batch_fiqa_saliency_calibration.ipynb`를 새 실행 진입점으로 사용한다. 모든 사용자 설정은 첫 코드 셀에 두고 기본 `EXECUTE=False`는 읽기 전용 사전 검증이다. `EXECUTE=True`는 입력 준비 → ① 공통 CI → ② 분할 안정성 → ③ 보고 통합을 실행한다.
2. `research/experiments/calibration_matrix.py`에 명시적 12개 완료 run을 고정했다. 자동 latest 선택은 하지 않는다. 같은 dataset에서 네 FR의 selected/alignment SHA, seed, evaluation 설정이 같아야 한다. 전체 full scope, checkpoint UID, freeze/prepared lineage, codec 및 ADC test core SHA를 검사한다.
3. `calibration_protocols.py`가 기존 Step-4의 protocol을 복원한다. LFW는 calibration gallery 수 제한과 enrollment 1, 기존 test identity 목록/enrollment를 유지한다. RFW-Custom은 test의 그룹별 gallery identity 수에 맞춘 calibration을 사용한다. SurvFace는 training matched calibration 및 official test를 유지한다. RFW-Official/TinyFace는 포함하지 않는다.
4. `replay_open_set_adc_condition_scores`는 기존 replay 함수의 호환 alias다. 기존 calibration ADC를 재생하고 persisted test core를 사용한다. LFW의 기존 threshold audit에는 당시 maximize-DIR 규칙을 사용하고, RFW-Custom/SurvFace는 non-mated-only 규칙을 재현한다. 새 FIQA/saliency fitting은 세 데이터셋 모두 fit non-mated에서만 수행한다.
5. FIQA는 dataset/alignment/checkpoint가 같으면 재사용한다. LFW/RFW-Custom의 기존 saliency target은 `origin_leave_one_out_identity_cosine`로 실제 확인되었다. 이 label-dependent target은 새 보정 입력으로 재사용하지 않고 calibration/test 모두 label-free `origin_top1_gallery_cosine`으로 생성한다. 생성은 CUDA 필수이며 CPU 자동 fallback은 없다.
6. SurvFace m128의 네 완료 saliency 입력을 명시적 override로 검증 후 재사용한다. 기존 generation spec/hash는 그대로 남긴다. 생성 옵션이 바뀌면 override와 충돌하므로 제거 후 새 UID로 생성해야 한다. 이전 과학 코드의 미완료 shard는 새 multi-dataset generation contract와 호환된다고 간주하지 않는다.
7. `saliency_condition_binding.py`는 동일 immutable run/prepared/alignment/protocol seed 및 동일 query identity·is_mated·순서를 검증한 뒤 세 PQ budget에 origin saliency를 연결한다. 새 디렉터리에 compact CSV를 복사하고 원본 manifest hash/condition hash 및 연결 사유를 기록한다. 원본 입력을 수정하거나 새로 추론한 것처럼 provenance를 바꾸지 않는다.
8. 비교는 Global-safe, FIQA 2-bin, FIQA 5-bin, Continuous FIQA, +outside, +entropy, +both다. 01과 02에서 다시 적합한 Continuous FIQA의 FPIR/TPIR가 정확히 일치해야 보고한다. Target 실패와 saliency invalid fallback/faithfulness failed를 숨기지 않는다.
9. Fit 결과는 방법군·condition·seed별 immutable artifact와 receipt로 저장한다. Input manifest, 설정, transitive science code SHA, numpy/pandas/scipy 버전을 포함한 job UID를 사용한다. 재실행은 receipt와 output SHA를 확인하고 적합 전에 재사용한다. 단일 split의 8972는 20개 seed panel에서도 재사용한다.
10. 보고는 조건별 raw metrics/paired comparisons/split summary를 저장하고 누락 job은 `partial`로 표시한다. 데이터셋을 pooling하거나 개선된 조건만 선택하지 않는다. 같은 test/gallery의 split min/median/max는 기술통계이며 독립 반복 CI가 아니다. PQ별 표는 나란히 비교하며 압축률 사이 paired CI를 추가 구현한 것은 아니다.

### Source run 고정 목록

| FR | LFW | RFW-Custom | SurvFace |
|---|---|---|---|
| ArcFace | 20260902-R001-6c8b08b1 | 20260903-R001-af875d0f | 20260902-R001-61915edf |
| AdaFace | 20260830-R001-2c810919 | 20260830-R001-9f3f1375 | 20260830-R001-ec6e5d4a |
| MagFace | 20260831-R001-937290ca | 20260831-R001-7c74c5f7 | 20260831-R001-6695386d |
| EdgeFace | 20260901-R001-03c98fe5 | 20260901-R001-0c811e8a | 20260901-R001-56c2f3ed |

### 검증 근거와 재실행 범위

- 36개 요청 조건 모두 원본 codec/ADC test core 사전 검증 통과. Dataset별 test query 수는 모든 모델/PQ에서 LFW 2,164, RFW-Custom 15,063, SurvFace 182,159였다. 이는 성능 수치가 아닌 입력 coverage다.
- 실제 ArcFace LFW m32 calibration 3,019행/test 2,164행 및 RFW-Custom m32 calibration 6,855행/test 15,063행을 재생했다. 각 5개 persisted threshold와 차이 0.0으로 재현했다.
- 위 두 데이터셋에서 calibration/test 각 64개 query, calibration 가림 표본 32개, CI 100회의 실제 GPU saliency smoke를 수행했다. Torch 2.7.1+cu118 / CUDA runtime 11.8 / GTX 1080 Ti. 입력 생성·CSV/SHA·가림 CI 재검산 통과; limited input의 정식 비교 gate는 올바르게 blocked다. `tmp/calibration_matrix_validation`의 검증 자료이며 정식 성능 결과가 아니다.
- 관련 76개 테스트 통과: 세 데이터셋의 baseline 일치와 test 변경 시 fitted model 불변, PQ budget 간 saliency 재사용과 source 불변, protocol/query 변경 거부, seed checkpoint 재사용과 변조 탐지, 누락/중복 보고, 기존 fallback·metric·CI 회귀를 포함한다.
- 새 노트북의 11개 셀을 기본 설정으로 새 커널 Run All하여 36조건 사전 검증 통과를 확인했다. 기존 00/01/02 노트북의 모든 code cell·설정·execution count·저장 출력이 HEAD와 동일한지 비교하여 보존을 확인했다. Ruff와 diff whitespace 검사 통과.
- SurvFace 네 모델의 기존 m128 saliency 입력을 현재 loader/gate로 다시 검증했다. 네 모델 모두 비교 가능하고 기존 faithfulness `failed` 상태를 그대로 유지한다.
- 결과 manifest에 dataset/PQ를 명시하고 보고 receipt의 dataset·PQ·source run·checkpoint·condition SHA·방법군·seed가 실제 결과와 일치하는지 확인한다. 압축률 이름만 바꿔 같은 결과를 중복 보고하는 경우를 차단한다. 빈/부분 보고도 `partial`로 저장·재검증한다.
- 재실행 대상: 누락된 FIQA(데이터셋별), LFW/RFW-Custom의 origin-top1 saliency(모델별), 누락된 condition calibration 점수, 36조건의 새 보정/CI/분할/보고. 기본 FR embedding, PCA/PQ fitting, 완료 run 및 DB를 다시 실행하지 않는다.

### 남은 위험과 현재 구현 경계

- 36조건의 정식 전체 성능 실행은 사용자 Run All 대상이며, 개선 효과를 아직 주장하지 않는다. 여러 조건/방법/목표의 CI는 다중 비교 보정이 없다.
- 적은 non-mated/identity 수에서 1% FPIR 또는 group-wise 결과의 불확실성이 크다. Empirical safety는 test FPIR 보장이 아니다.
- LFW/RFW-Custom 새 saliency 생성은 실제 추가 GPU 작업이며 오래 걸릴 수 있다. Full dataset saliency를 생성하지만 compact 입력을 세 budget에 공유한다.
- Source protocol 복원은 현재 완료 Step-4 계약에 한정한다. 완료 run에 누락/손상/비동질 source가 있으면 중단하며 새로 자동 대체하지 않는다.
- Saliency에는 원본 gallery가 필요하다. 압축 DB 단독 배포 및 추가 추론 비용 최적화는 이번 구현 범위가 아니다.

## 17:59 KST — Windows saliency binding 최종 저장 실패 보완

- 상태: **구현됨 / 회귀·실제 실패 조건 복구 및 재사용 검증됨**. 위 36조건 설계와 20260918의 immutable 입력·fallback 정책을 유지한다. 전체 성능 실험 완료를 의미하지 않는다.
- 배경: 공통 batch 입력 준비 중 AdaFace/SurvFace/PQ m64의 `os.rename(staging, destination)`에서 `PermissionError: WinError 5`가 발생했다. 최종 경로는 없고 검증 직후의 임시 폴더는 남아 있었다. 접근을 거부한 프로세스는 확인되지 않았다.
- 변경: `saliency_condition_binding.py`가 기존 `_publish_atomic_directory(..., overwrite=False)`를 사용한다. PermissionError에 최대 8회, 총 대기 6.35초의 재시도를 적용한다. 실패 지속 시 오류를 전달하고 임시 자료를 보존한다. 기존 완료 결과를 교체하거나 검증 gate를 완화하지 않는다.
- 흐름: 원본 완료 입력 검증 → 동일 cohort의 PQ 조건 연결·복사 → 무결성 gate → 재시도 가능한 최종 저장 → 저장된 입력 재검증. 추론·threshold·평가 프로토콜·DB 변경은 없다. 노트북 파일과 사용자의 `EXECUTE=True` 설정 및 저장 출력은 수정하지 않았다.
- 검증: `test_calibration_matrix.py` 및 `test_population_saliency_pipeline.py`의 16개 테스트 통과. 일시 잠금 뒤 성공, 지속 잠금 시 중단·임시 자료 보존, 원본 불변, 완료 연결 재사용을 확인했다. 노트북 검사는 실행 스위치의 실제 사용자 선택을 허용한다. Ruff와 diff whitespace 검사 통과.
- 실제 자료: AdaFace m128 입력을 m64에 연결해 calibration 160,408행/test 182,159행의 gate=ready와 동일 결과 재사용을 확인했다. 새 연결은 `results/calibration/saliency_bindings/20260830-R001-ec6e5d4a/saliency-binding-446f52ec207819bbd1dcae91`이다. 원본 saliency/faithfulness와 기존 실패 staging 파일들의 SHA-256이 전후 동일하다. GPU 추론이나 threshold fitting은 실행하지 않았다. 기존 faithfulness=failed, invalid saliency calibration 1개/test 2개의 FIQA-only fallback을 그대로 유지한다.
- 재실행 범위: Kernel Restart → Run All로 변경 모듈을 반영한다. 완료 FIQA·condition scores·saliency 입력을 검증 후 재사용하고 남은 입력·비교 작업을 수행한다. 연결 모듈 SHA 변경에 따라 기존 PQ 연결의 compact 복사본은 새 UID로 작성될 수 있으나 원본 saliency 재추론은 필요 없다.
- 한계: 영구적인 권한 문제나 재시도 시간을 넘는 외부 잠금은 여전히 명시적 오류로 중단한다. 별도의 GPU 환경 변경이나 관리자 실행을 요구하지 않는다.

## 21:17 KST — 보정 결과·통합 보고 저장 재시도와 기존 668개 작업 복구

- 상태: **구현됨 / 회귀 및 새 Jupyter 커널의 실제 재사용 검증됨**. 17:59의 입력 연결 저장 보완을 유지하면서, 당시 빠졌던 보정 결과와 matrix 보고 저장까지 보완한다. 수학적 모델·평가·입력 생성 계약은 유지한다.
- 배경: 단일 분할의 36조건 보고는 `matrix-report-2e2ab02ddb1d039ef1084383`으로 완료됐다. 이후 20분할 실행의 AdaFace/SurvFace/m128/seed 12 saliency 결과 저장이 `saliency_incremental_calibration.py`의 `os.rename`에서 WinError 5로 중단됐다. 이전 입력 연결 오류와 다른 저장 지점이다. 당시 정상 receipt는 667개였고, 추가 1개는 모든 CSV와 completed manifest를 쓴 staging 상태였다.
- 변경 코드: `fiqa_continuous_calibration.py`, `saliency_incremental_calibration.py`, `calibration_matrix.py`의 최종 디렉터리 저장을 기존 `_publish_atomic_directory(..., overwrite=False)`로 통일했다. 세 파일의 변경은 import 및 저장 호출 교체뿐이다. FIQA/saliency fitting, safety offset, CI, fallback, FPIR/TPIR 계산, DB는 변경하지 않았다. 영구 권한 오류는 재시도 한도 이후 여전히 중단한다.
- 재사용 문제: matrix job UID가 모듈 전체 SHA를 포함하므로 저장 처리만 고쳐도 완료 결과를 다시 적합할 수 있다. `scripts/recover_calibration_matrix_publish.py`를 추가해 기준 commit `672b7b799aa58c585047b720f1925e6e27451b1e` 대비 정확한 세 저장 호출 변경만 허용하고, 나머지 implementation·입력·설정·런타임은 일치해야 재사용하도록 했다. 기본은 읽기 전용이며 `--apply`가 복구를 수행한다. 자동 latest 선택이나 임의 hash 무시는 없다.
- 복구 흐름: 기존 receipt·job UID·result UID·전체 CSV 해시·input manifest SHA·seed·설정·implementation·런타임 검증 → 중단된 staging은 반대 방법군의 검증된 spec에서 정확한 기존 job UID를 복원해 검증 → 최종 저장 및 누락 receipt 작성 → 새 코드 SHA의 job receipt가 검증된 기존 결과 디렉터리를 참조한다. 원본 결과 manifest의 실제 producer SHA와 수치는 그대로 유지하고, 새 receipt에 원본 receipt/spec 및 복구 코드 SHA를 기록한다.
- 실제 복구: `job-6a677d8bd1407c62d136af57/saliency-incremental-7dff2a13afdb279d8f66426c`를 최종 저장하고 총 668개 고유 결과에 대한 재사용 receipt를 작성했다. 기존 667개 receipt는 전후 해시가 동일하다. 기존+새 참조 receipt가 1,336개여도 실험 결과는 668개이며, 이중 계산/평가하지 않는다. 원본 report도 변경하지 않았다.
- 복구 감사 기록: `results/calibration/matrix/publish_recovery/recovery-d6d1adfe1f15fc04d0267337.json`. 복구 코드 SHA `04bc898035fed0ef04773b8324e99aba608e87fd8307212746241607b1150c56`. Git blob과 작업 트리의 혼합 LF/CRLF 차이도 역변환하여 기존 FIQA 코드 SHA를 확인했다.
- 검증: 관련 50개 테스트 통과. 두 결과 writer와 matrix report의 일시 잠금 재시도, 지속 잠금 시 staging 보존, 완료 결과 불변, 추가 과학 코드 변경 거부, 복구 후 재적합 차단 및 반복 복구를 검증했다. Ruff와 diff whitespace 검사 통과.
- 새 Python 3.11 Jupyter 커널에서 36조건 사전 검증 통과. 실제 AdaFace/SurvFace/m128의 seed 8972·12, 두 방법군 총 4개 작업이 모두 `reuse=True`였으며 입력 생성/적합 함수를 호출하면 실패하도록 guard를 둬 재계산 0회를 확인했다. 검증 출력은 `tmp/matrix_publish_resume_verified.ipynb`이다. 사용자 원본 노트북의 설정·출력 bytes는 전후 동일하다.
- 재실행: 현재 설정을 유지하고 Kernel Restart → Run All. 완료 입력과 복구된 668개 결과를 검증 후 재사용하고 남은 분할을 실행한다. 전체 36조건×20분할의 완료나 saliency 개선 효과를 주장하지 않는다. 이 복구는 명시적으로 감사한 저장 전용 수정에 한정되며, 향후 과학 코드·입력·설정 변경은 별도 결과로 계산해야 한다.
