# 논문 연구 방향 정리

## 1. 연구 방향

본 연구의 중심은 **PostgreSQL/pgvector 기반 인물 이미지 검색에서 얼굴 전용 임베딩의 압축 가능성과 검색 효율을 분석하는 것**이다. 기존 방향처럼 segmentation 기반 영역 가중 정규화를 핵심 novelty로 두지 않고, 현재 프로젝트의 구현 기반과 가장 잘 맞는 `ArcFace` 얼굴 전용 임베딩을 주 실험 대상으로 삼는다.

연구의 핵심 문제는 다음과 같다.

> ArcFace 얼굴 임베딩은 PCA/PQ 압축 이후에도 identity 검색 성능을 유지하면서 저장공간과 질의시간을 줄일 수 있는가?

따라서 본 연구는 단순히 여러 압축 기법을 나열해 비교하는 것이 아니라, 실제 관계형 데이터베이스 환경인 PostgreSQL/pgvector에서 얼굴 전용 임베딩을 압축했을 때 정확도, 질의 시간, 저장공간, HNSW 인덱스 성능이 어떤 trade-off를 보이는지 정량적으로 분석한다.

## 2. 핵심 Novelty

본 연구의 핵심 novelty는 다음 세 가지로 정리한다.

1. **얼굴 전용 임베딩의 실용적 압축 가능성 분석**
   - ArcFace 512D 임베딩을 기준으로 PCA 및 PQ 압축을 적용했을 때 검색 성능이 얼마나 유지되는지 평가한다.
   - 단순 정확도 비교가 아니라 압축률, 질의 시간, 저장공간 절감 효과를 함께 분석한다.

2. **PostgreSQL/pgvector 환경에서의 검색 효율 분석**
   - 전용 vector database가 아닌 PostgreSQL 기반 시스템에서 HNSW 검색이 어떤 성능 특성과 한계를 보이는지 확인한다.
   - 실제 서비스형 관계형 DB 환경에서 인물 이미지 검색 시스템을 구성할 때의 실용적 판단 기준을 제시한다.

3. **일반 이미지 모델과의 ablation 비교**
   - 일반 이미지 모델은 본 연구의 중심 모델이 아니라 비교군으로만 사용한다.
   - ArcFace와 일반 이미지 모델의 검색 결과를 비교해 일반 모델이 배경, 옷, 자세 등 비-identity 단서에 어떤 방식으로 흔들리는지 분석한다.

본 연구의 novelty는 "새로운 정규화 수식을 제안했다"가 아니라, **얼굴 전용 임베딩을 실제 PostgreSQL/pgvector 검색 시스템에서 압축해도 실용적인 성능을 유지할 수 있는지 검증한다**는 점에 있다.

## 3. 연구 질문

### 핵심 연구 질문

> PostgreSQL/pgvector 기반 인물 이미지 검색에서 ArcFace 임베딩의 PCA/PQ 압축은 검색 정확도를 유지하면서 저장공간과 질의시간을 줄일 수 있는가?

### 보조 연구 질문

1. ArcFace 512D 원본 임베딩 대비 PCA-256D 압축 임베딩은 어느 정도의 Recall과 mAP를 유지하는가?
2. PCA-256D 이후 PQ를 적용하면 저장공간과 질의시간은 얼마나 줄어들고, 정확도 손실은 어느 정도 발생하는가?
3. PostgreSQL/pgvector의 HNSW 검색은 Brute Force 기준 검색 결과와 비교했을 때 어떤 정확도 및 시간 trade-off를 보이는가?
4. 일반 이미지 모델은 ArcFace와 비교했을 때 배경, 옷, 자세 같은 비-identity 요소에 얼마나 영향을 받는가?
5. 압축된 얼굴 전용 임베딩은 실제 관계형 DB 기반 이미지 검색 시스템에서 사용할 수 있을 만큼 실용적인가?

## 4. 실험군 구성

실험군은 핵심 메시지가 흐려지지 않도록 4개로 제한한다.

| 실험군 | 설명 | 목적 |
| --- | --- | --- |
| ArcFace 512D | ArcFace 원본 얼굴 임베딩 | 얼굴 전용 모델 기준선 |
| ArcFace PCA-256D | ArcFace 임베딩을 PCA로 256차원 축소 | 실용적 압축 기준선 |
| ArcFace PCA-256D + PQ | PCA-256D 벡터에 PQ 양자화 적용 | 강한 압축에서 정확도-효율 trade-off 확인 |
| General Model 512D | 일반 이미지 모델의 원본 임베딩 | ablation 및 실패 양상 비교 |

이 구성은 연구 변수를 다음처럼 단순화한다.

- 주 모델: ArcFace
- 주 압축 방식: PCA, PQ
- 비교 모델: 일반 이미지 모델 1개
- 비교 목적: 성능 경쟁이 아니라 실패 양상 분석

## 5. 평가 지표

평가 지표는 검색 정확도, 시스템 효율, 압축 효과를 분리해 측정한다.

### 검색 정확도

- `Recall@1`
- `Recall@5`
- `Recall@K`
- `mAP`

### 시스템 효율

- `Query Time`
- `Storage Size`
- `Index Build Time`

### 압축 분석

- `Compression Ratio`
- `Accuracy Loss`
- 원본 대비 성능 유지율

### 보조 통합 지표

`NRES`는 핵심 novelty가 아니라 검색 시스템의 실용 효율을 요약하는 보조 지표로 사용한다.

```text
A_norm = Recall_compressed / Recall_original
T_norm = Time_original / Time_compressed
S_norm = Storage_original / Storage_compressed

NRES = A_norm^0.5 * T_norm^0.25 * S_norm^0.25
```

정확도를 가장 중요한 요소로 두기 위해 `A_norm`의 가중치를 가장 크게 둔다. 다만 최종 결론은 NRES 하나에만 의존하지 않고 Recall, mAP, query time, storage 결과를 함께 해석한다.

## 6. 일반 이미지 모델 Ablation의 역할

일반 이미지 모델은 본 연구의 주 모델이 아니다. 역할은 ArcFace와의 대비를 통해 얼굴 전용 임베딩의 필요성과 실패 양상 차이를 설명하는 것이다.

일반 이미지 모델 비교에서 확인할 내용은 다음과 같다.

1. 같은 사람이라도 배경이나 옷이 달라질 때 검색 성능이 흔들리는가?
2. 다른 사람이라도 같은 배경, 비슷한 옷, 비슷한 자세를 가지면 유사하게 검색되는가?
3. ArcFace는 일반 이미지 모델보다 identity 중심의 검색 결과를 안정적으로 유지하는가?
4. 일반 이미지 모델은 인물 검색에서 어떤 조건에서 실패하는가?

따라서 일반 이미지 모델 실험의 결론은 "일반 모델이 나쁘다"가 아니라, **얼굴 전용 모델과 일반 모델이 서로 다른 단서에 의존하며, 인물 identity 검색에는 얼굴 전용 임베딩이 더 적합하다**는 방향으로 해석한다.

## 7. 제외하거나 축소할 요소

이번 연구의 핵심에서 제외하거나 축소할 요소는 다음과 같다.

1. **Segmentation 기반 영역 가중 정규화**
   - 일반 이미지 모델을 주 모델로 삼을 경우에는 의미 있는 novelty가 될 수 있다.
   - 그러나 본 연구는 ArcFace 얼굴 전용 임베딩을 중심으로 하므로 핵심 주장에서 제외한다.
   - 필요하면 향후 연구 또는 일반 이미지 모델 보정 방향으로만 언급한다.

2. **DCT와 Wavelet**
   - 현재 프로젝트에는 DCT/Wavelet 실험 기반이 있지만, 본 논문 핵심에 포함하면 실험군이 과도하게 늘어난다.
   - PCA/PQ만으로도 압축과 검색 효율 분석의 핵심 메시지를 충분히 만들 수 있다.
   - DCT/Wavelet은 추가 실험 후보 또는 향후 연구로 둔다.

3. **새로운 정규화 수식 자체를 novelty로 주장하는 방식**
   - NRES 같은 통합 지표는 평가 편의를 위한 보조 지표로 사용한다.
   - 새로운 수학 이론이나 일반화된 정규화 기법처럼 과도하게 주장하지 않는다.

## 8. 최종 논문 주장

가장 안전한 최종 논문 주장은 다음과 같다.

> 본 연구는 PostgreSQL/pgvector 기반 인물 이미지 검색에서 ArcFace 얼굴 전용 임베딩의 PCA/PQ 압축이 검색 정확도, 질의 시간, 저장공간에 미치는 영향을 분석하고, 일반 이미지 모델과의 ablation을 통해 얼굴 전용 임베딩의 identity 검색 안정성과 일반 모델의 실패 양상을 실험적으로 검증한다.

논문 제목 후보는 다음과 같다.

1. `PostgreSQL/pgvector 기반 인물 이미지 검색에서 얼굴 전용 임베딩 압축의 검색 효율 분석`
2. `ArcFace 임베딩 압축을 활용한 PostgreSQL 기반 인물 이미지 검색 성능 분석`
3. `관계형 데이터베이스 환경에서 얼굴 임베딩 압축이 인물 이미지 검색 성능에 미치는 영향`

이 중 1번 제목이 연구 환경, 대상, novelty를 가장 명확하게 드러낸다.

## 9. 진행 순서

실험과 논문 정리는 다음 순서로 진행한다.

1. **ArcFace 512D 기준선 구축**
   - 이미지별 ArcFace 원본 임베딩을 추출하고 PostgreSQL/pgvector에 저장한다.
   - Brute Force와 HNSW 검색 결과를 비교해 기준 성능을 확보한다.

2. **PCA-256D 압축 실험**
   - ArcFace 512D 임베딩을 PCA-256D로 축소한다.
   - 원본 대비 Recall, mAP, query time, storage 변화를 측정한다.

3. **PCA-256D + PQ 압축 실험**
   - PCA-256D 벡터에 PQ 양자화를 적용한다.
   - 강한 압축에서 정확도 손실과 시스템 효율 개선 폭을 확인한다.

4. **PostgreSQL/pgvector HNSW 분석**
   - Brute Force 결과를 기준으로 HNSW 검색의 정확도와 질의 시간을 비교한다.
   - `K`, `ef_search`, 인덱스 구성에 따른 성능 변화를 분석한다.

5. **일반 이미지 모델 ablation**
   - 일반 이미지 모델 512D 임베딩을 추출해 ArcFace와 비교한다.
   - 같은 배경, 비슷한 옷, 자세 변화 등에서 어떤 실패 양상이 나타나는지 정성적으로 분석한다.

6. **결론 정리**
   - ArcFace 압축 임베딩의 실용 가능성을 정량적으로 제시한다.
   - 일반 이미지 모델은 인물 identity 검색에서 어떤 한계를 보이는지 보조적으로 설명한다.
   - DCT/Wavelet, segmentation 기반 보정은 향후 연구로 정리한다.
